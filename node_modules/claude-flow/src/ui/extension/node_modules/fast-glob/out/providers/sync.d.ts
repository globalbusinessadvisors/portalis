import { Task } from '../managers/tasks.js';
import ReaderSync from '../readers/sync.js';
import { Entry, EntryItem, ReaderOptions } from '../types.js';
import Provider from './provider.js';
export default class ProviderSync extends Provider<EntryItem[]> {
    protected _reader: ReaderSync;
constructor(task: Task): EntryItem[];
constructor(root: string, task: Task, options: ReaderOptions): Entry[];
}
