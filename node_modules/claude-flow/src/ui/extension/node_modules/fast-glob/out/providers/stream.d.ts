/// <reference types="node" />
import { Readable } from 'stream';
import { Task } from '../managers/tasks.js';
import ReaderStream from '../readers/stream.js';
import { ReaderOptions } from '../types.js';
import Provider from './provider.js';
export default class ProviderStream extends Provider<Readable> {
    protected _reader: ReaderStream;
constructor(task: Task): Readable;
constructor(root: string, task: Task, options: ReaderOptions): Readable;
}
