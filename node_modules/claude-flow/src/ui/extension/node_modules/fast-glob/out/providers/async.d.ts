import { Task } from '../managers/tasks.js';
import { Entry, EntryItem, ReaderOptions } from '../types.js';
import ReaderAsync from '../readers/async.js';
import Provider from './provider.js';
export default class ProviderAsync extends Provider<Promise<EntryItem[]>> {
    protected _reader: ReaderAsync;
constructor(task: Task): Promise<EntryItem[]>;
constructor(root: string, task: Task, options: ReaderOptions): Promise<Entry[]>;
}
