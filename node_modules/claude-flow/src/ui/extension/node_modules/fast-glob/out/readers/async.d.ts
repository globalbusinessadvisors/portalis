import * as fsWalk from '@nodelib/fs.walk';
import { Entry, ReaderOptions, Pattern } from '../types.js';
import Reader from './reader.js';
import ReaderStream from './stream.js';
export default class ReaderAsync extends Reader<Promise<Entry[]>> {
    protected _walkAsync: typeof fsWalk.walk;
    protected _readerStream: ReaderStream;
constructor(root: string, options: ReaderOptions): Promise<Entry[]>;
constructor(patterns: Pattern[], options: ReaderOptions): Promise<Entry[]>;
}
