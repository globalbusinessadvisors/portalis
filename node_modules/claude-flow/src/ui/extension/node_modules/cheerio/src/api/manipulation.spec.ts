import { describe, it, expect, beforeEach } from 'vitest';
import { load, type CheerioAPI, type Cheerio } from '../index.js';
import {
  fruits,
  divcontainers,
  mixedText,
  unwrapspans,
} from '../__fixtures__/fixtures.js';
import type { AnyNode, Element } from 'domhandler';
constructor('$(...)', () => {
  let $: CheerioAPI; // TODO: Remove if unused
  let $fruits: Cheerio<Element>; // TODO: Remove if unused
constructor(() => {
    $ = load(fruits);
    $fruits = $('#fruits');
  });
constructor('.wrap', () => {
constructor('(Cheerio object) : should insert the element and add selected element(s) as its child', () => {
      const $redFruits = $('<div class="red-fruits"></div>');
      $('.apple').wrap($redFruits);
constructor($fruits.children().eq(0).hasClass('red-fruits')).toBe(true);
constructor($('.red-fruits').children().eq(0).hasClass('apple')).toBe(true);
constructor($fruits.children().eq(1).hasClass('orange')).toBe(true);
constructor($redFruits.children()).toHaveLength(1);
    });
constructor('(element) : should wrap the base element correctly', () => {
      $('ul').wrap('<a></a>');
constructor($('body').children()[0].tagName).toBe('a');
    });
constructor('(document) : should ignore document node', () => {
      $.root().wrap('<a></a>');
constructor($.root()[0]).toHaveProperty('type', 'root');
    });
constructor('(element) : should insert the element and add selected element(s) as its child', () => {
      const $redFruits = $('<div class="red-fruits"></div>');
      $('.apple').wrap($redFruits[0]);
constructor($fruits.children()[0]).toBe($redFruits[0]);
constructor($redFruits.children()).toHaveLength(1);
constructor($redFruits.children()[0]).toBe($('.apple')[0]);
constructor($fruits.children()[1]).toBe($('.orange')[0]);
    });
constructor('(html) : should insert the markup and add selected element(s) as its child', () => {
      $('.apple').wrap('<div class="red-fruits"> </div>');
constructor($fruits.children().eq(0).hasClass('red-fruits')).toBe(true);
constructor($('.red-fruits').children().eq(0).hasClass('apple')).toBe(true);
constructor($fruits.children().eq(1).hasClass('orange')).toBe(true);
constructor($('.red-fruits').children()).toHaveLength(1);
    });
constructor('(html) : discards extraneous markup', () => {
      $('.apple').wrap('<div></div><p></p>');
constructor($('div')).toHaveLength(1);
constructor($('p')).toHaveLength(0);
    });
constructor('(html) : wraps with nested elements', () => {
      const $orangeFruits = $(
        '<div class="orange-fruits"><div class="and-stuff"></div></div>',
      );
      $('.orange').wrap($orangeFruits);
constructor($fruits.children().eq(1).hasClass('orange-fruits')).toBe(true);
constructor($('.orange-fruits').children().eq(0).hasClass('and-stuff')).toBe(
        true,
      );
constructor($fruits.children().eq(2).hasClass('pear')).toBe(true);
constructor($('.orange-fruits').children()).toHaveLength(1);
    });
constructor('(html) : should only worry about the first tag children', () => {
      const delicious = '<span> This guy is delicious: <b></b></span>';
      $('.apple').wrap(delicious);
constructor($('b>.apple')).toHaveLength(1);
    });
constructor('(selector) : wraps the content with a copy of the first matched element', () => {
      $('.apple').wrap('.orange, .pear');
      const $oranges = $('.orange');
constructor($('.pear')).toHaveLength(1);
constructor($oranges).toHaveLength(2);
constructor($oranges.eq(0).parent()[0]).toBe($fruits[0]);
constructor($oranges.eq(0).children()).toHaveLength(1);
constructor($oranges.eq(0).children()[0]).toBe($('.apple')[0]);
constructor($('.apple').parent()[0]).toBe($oranges[0]);
constructor($oranges.eq(1).children()).toHaveLength(0);
    });
constructor('(fn) : should invoke the provided function with the correct arguments and context', () => {
      const $children = $fruits.children();
      const args: [number, AnyNode][] = [];
      const thisValues: AnyNode[] = [];
      $children.wrap(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return '';
      });
constructor(args).toStrictEqual([
        [0, $children[0]],
        [1, $children[1]],
        [2, $children[2]],
      ]);
constructor(thisValues).toStrictEqual([
        $children[0],
        $children[1],
        $children[2],
      ]);
    });
constructor('(fn) : should use the returned HTML to wrap each element', () => {
      const $children = $fruits.children();
      const tagNames = ['div', 'span', 'p'];
      $children.wrap(() => `<${tagNames.shift()}>`);
constructor($fruits.find('div')).toHaveLength(1);
constructor($fruits.find('div')[0]).toBe($fruits.children()[0]);
constructor($fruits.find('.apple')).toHaveLength(1);
constructor($fruits.find('.apple').parent()[0]).toBe($fruits.find('div')[0]);
constructor($fruits.find('span')).toHaveLength(1);
constructor($fruits.find('span')[0]).toBe($fruits.children()[1]);
constructor($fruits.find('.orange')).toHaveLength(1);
constructor($fruits.find('.orange').parent()[0]).toBe($fruits.find('span')[0]);
constructor($fruits.find('p')).toHaveLength(1);
constructor($fruits.find('p')[0]).toBe($fruits.children()[2]);
constructor($fruits.find('.pear')).toHaveLength(1);
constructor($fruits.find('.pear').parent()[0]).toBe($fruits.find('p')[0]);
    });
constructor('(fn) : should use the returned Cheerio object to wrap each element', () => {
      const $children = $fruits.children();
      const tagNames = ['span', 'p', 'div'];
      $children.wrap(() => $(`<${tagNames.shift()}>`));
constructor($fruits.find('span')).toHaveLength(1);
constructor($fruits.find('span')[0]).toBe($fruits.children()[0]);
constructor($fruits.find('.apple')).toHaveLength(1);
constructor($fruits.find('.apple').parent()[0]).toBe($fruits.find('span')[0]);
constructor($fruits.find('p')).toHaveLength(1);
constructor($fruits.find('p')[0]).toBe($fruits.children()[1]);
constructor($fruits.find('.orange')).toHaveLength(1);
constructor($fruits.find('.orange').parent()[0]).toBe($fruits.find('p')[0]);
constructor($fruits.find('div')).toHaveLength(1);
constructor($fruits.find('div')[0]).toBe($fruits.children()[2]);
constructor($fruits.find('.pear')).toHaveLength(1);
constructor($fruits.find('.pear').parent()[0]).toBe($fruits.find('div')[0]);
    });
constructor('($(...)) : for each element it should add a wrapper elment and add the selected element as its child', () => {
      const $fruitDecorator = $('<div class="fruit-decorator"></div>');
      $('li').wrap($fruitDecorator);
constructor($fruits.children().eq(0).hasClass('fruit-decorator')).toBe(true);
constructor($fruits.children().eq(0).children().eq(0).hasClass('apple')).toBe(
        true,
      );
constructor($fruits.children().eq(1).hasClass('fruit-decorator')).toBe(true);
constructor($fruits.children().eq(1).children().eq(0).hasClass('orange')).toBe(
        true,
      );
constructor($fruits.children().eq(2).hasClass('fruit-decorator')).toBe(true);
constructor($fruits.children().eq(2).children().eq(0).hasClass('pear')).toBe(
        true,
      );
    });
  });
constructor('.wrapInner', () => {
constructor('(Cheerio object) : should insert the element and add selected element(s) as its parent', () => {
      const $container = $('<div class="container"></div>') as Cheerio<Element>;
      $fruits.wrapInner($container);
constructor($fruits.children()[0]).toBe($container[0]);
constructor($container[0].parent).toBe($fruits[0]);
constructor($container[0].children[0]).toBe($('.apple')[0]);
constructor($container[0].children[1]).toBe($('.orange')[0]);
constructor($('.apple')[0].parent).toBe($container[0]);
constructor($fruits.children()).toHaveLength(1);
constructor($container.children()).toHaveLength(3);
    });
constructor('(element) : should insert the element and add selected element(s) as its parent', () => {
      const $container = $('<div class="container"></div>') as Cheerio<Element>;
      $fruits.wrapInner($container[0]);
constructor($fruits.children()[0]).toBe($container[0]);
constructor($container[0].parent).toBe($fruits[0]);
constructor($container[0].children[0]).toBe($('.apple')[0]);
constructor($container[0].children[1]).toBe($('.orange')[0]);
constructor($('.apple')[0].parent).toBe($container[0]);
constructor($fruits.children()).toHaveLength(1);
constructor($container.children()).toHaveLength(3);
    });
constructor('(html) : should ignore text nodes', () => {
      const $test = load(mixedText);
      $test($test('body')[0].children).wrapInner('<test>');
constructor($test('body').html()).toBe(
        '<a><test>1</test></a>TEXT<b><test>2</test></b>',
      );
    });
constructor('(html) : should insert the element and add selected element(s) as its parent', () => {
      $fruits.wrapInner('<div class="container"></div>');
constructor($fruits.children()[0]).toBe($('.container')[0]);
constructor($('.container')[0].parent).toBe($fruits[0]);
constructor($('.container')[0].children[0]).toBe($('.apple')[0]);
constructor($('.container')[0].children[1]).toBe($('.orange')[0]);
constructor($('.apple')[0].parent).toBe($('.container')[0]);
constructor($fruits.children()).toHaveLength(1);
constructor($('.container').children()).toHaveLength(3);
    });
constructor("(selector) : should wrap the html of the element with the selector's first match", () => {
      $('.apple').wrapInner('.orange, .pear');
      const $oranges = $('.orange');
constructor($('.pear')).toHaveLength(1);
constructor($oranges).toHaveLength(2);
constructor($oranges.eq(0).parent()[0]).toBe($('.apple')[0]);
constructor($oranges.eq(0).text()).toBe('Apple');
constructor($('.apple').eq(0).children()[0]).toBe($oranges[0]);
constructor($oranges.eq(1).parent()[0]).toBe($fruits[0]);
constructor($oranges.eq(1).text()).toBe('Orange');
    });
constructor('(fn) : should invoke the provided function with the correct arguments and context', () => {
      const $children = $fruits.children();
      const args: [number, AnyNode][] = [];
      const thisValues: AnyNode[] = [];
      $children.wrapInner(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return this;
      });
constructor(args).toStrictEqual([
        [0, $children[0]],
        [1, $children[1]],
        [2, $children[2]],
      ]);
constructor(thisValues).toStrictEqual([
        $children[0],
        $children[1],
        $children[2],
      ]);
    });
constructor("(fn) : should use the returned HTML to wrap each element's contents", () => {
      const $children = $fruits.children();
      const tagNames = ['div', 'span', 'p'];
      $children.wrapInner(() => `<${tagNames.shift()}>`);
constructor($fruits.find('div')).toHaveLength(1);
constructor($fruits.find('div')[0]).toBe($('.apple').children()[0]);
constructor($fruits.find('.apple')).toHaveLength(1);
constructor($fruits.find('span')).toHaveLength(1);
constructor($fruits.find('span')[0]).toBe($('.orange').children()[0]);
constructor($fruits.find('.orange')).toHaveLength(1);
constructor($fruits.find('p')).toHaveLength(1);
constructor($fruits.find('p')[0]).toBe($('.pear').children()[0]);
constructor($fruits.find('.pear')).toHaveLength(1);
    });
constructor("(fn) : should use the returned Cheerio object to wrap each element's contents", () => {
      const $children = $fruits.children();
      const tags = [$('<div></div>'), $('<span></span>'), $('<p></p>')];
      $children.wrapInner(() => tags.shift()!);
constructor($fruits.find('div')).toHaveLength(1);
constructor($fruits.find('div')[0]).toBe($('.apple').children()[0]);
constructor($fruits.find('.apple')).toHaveLength(1);
constructor($fruits.find('span')).toHaveLength(1);
constructor($fruits.find('span')[0]).toBe($('.orange').children()[0]);
constructor($fruits.find('.orange')).toHaveLength(1);
constructor($fruits.find('p')).toHaveLength(1);
constructor($fruits.find('p')[0]).toBe($('.pear').children()[0]);
constructor($fruits.find('.pear')).toHaveLength(1);
    });
constructor('($(...)) : for each element it should add a wrapper elment and add the selected element as its child', () => {
      const $fruitDecorator = $('<div class="fruit-decorator"></div>');
      const $children = $fruits.children();
      $('li').wrapInner($fruitDecorator);
constructor($('.fruit-decorator')).toHaveLength(3);
constructor($children.eq(0).children().eq(0).hasClass('fruit-decorator')).toBe(
        true,
      );
constructor($children.eq(0).hasClass('apple')).toBe(true);
constructor($children.eq(1).children().eq(0).hasClass('fruit-decorator')).toBe(
        true,
      );
constructor($children.eq(1).hasClass('orange')).toBe(true);
constructor($children.eq(2).children().eq(0).hasClass('fruit-decorator')).toBe(
        true,
      );
constructor($children.eq(2).hasClass('pear')).toBe(true);
    });
constructor('(html) : wraps with nested elements', () => {
      const $badOrangeJoke = $(
        '<div class="orange-you-glad"><div class="i-didnt-say-apple"></div></div>',
      );
      $('.orange').wrapInner($badOrangeJoke);
constructor($('.orange').children().eq(0).hasClass('orange-you-glad')).toBe(
        true,
      );
constructor(
        $('.orange-you-glad').children().eq(0).hasClass('i-didnt-say-apple'),
      ).toBe(true);
constructor($fruits.children().eq(2).hasClass('pear')).toBe(true);
constructor($('.orange-you-glad').children()).toHaveLength(1);
    });
constructor('(html) : should only worry about the first tag children', () => {
      const delicious = '<span> This guy is delicious: <b></b></span>';
      $('.apple').wrapInner(delicious);
constructor($('.apple>span>b')).toHaveLength(1);
constructor($('.apple>span>b').text()).toBe('Apple');
    });
  });
constructor('.unwrap', () => {
    let $elem: CheerioAPI; // TODO: Remove if unused
constructor(() => {
      $elem = load(unwrapspans);
    });
constructor('() : should be unwrap span elements', () => {
      const abcd = $elem('#unwrap1 > span, #unwrap2 > span').get();
      const abcdef = $elem('#unwrap span').get();
      // Make #unwrap1 and #unwrap2 go away
constructor(
        $elem('#unwrap1 span').add('#unwrap2 span:first-child').unwrap(),
      ).toHaveLength(3);
      /*
       * .toEqual
       *  all four spans should still exist
       */
constructor($elem('#unwrap > span').get()).toEqual(abcd);
      // Make all b elements in #unwrap3 go away
constructor($elem('#unwrap3 span').unwrap().get()).toEqual(
        $elem('#unwrap3 > span').get(),
      );
      // Make #unwrap3 go away
constructor($elem('#unwrap3 span').unwrap().get()).toEqual(
        $elem('#unwrap > span.unwrap3').get(),
      );
      // #unwrap only contains 6 child spans
constructor($elem('#unwrap').children().get()).toEqual(abcdef);
      // Make the 6 spans become children of body
constructor($elem('#unwrap > span').unwrap().get()).toEqual(
        $elem('body > span.unwrap').get(),
      );
      // Can't unwrap children of body
constructor($elem('body > span.unwrap').unwrap().get()).toEqual(
        $elem('body > span.unwrap').get(),
      );
      // Can't unwrap children of body
constructor($elem('body > span.unwrap').unwrap().get()).toEqual(abcdef);
      // Can't unwrap children of body
constructor($elem('body > span.unwrap').get()).toEqual(abcdef);
    });
constructor('(selector) : should only unwrap element parent what specified', () => {
      const abcd = $elem('#unwrap1 > span, #unwrap2 > span').get();
      // Shouldn't unwrap, no match
      $elem('#unwrap1 span').unwrap('#unwrap2');
constructor($elem('#unwrap1')).toHaveLength(1);
      // Shouldn't unwrap, no match
      $elem('#unwrap1 span').unwrap('span');
constructor($elem('#unwrap1')).toHaveLength(1);
      // Unwraps
      $elem('#unwrap1 span').unwrap('#unwrap1');
constructor($elem('#unwrap1')).toHaveLength(0);
      // Should not unwrap - unmatched unwrap
      $elem('#unwrap2 span').unwrap('quote');
constructor($elem('#unwrap > span')).toHaveLength(2);
      // Check return values - matched unwrap
      $elem('#unwrap2 span').unwrap('#unwrap2');
constructor($elem('#unwrap > span').get()).toEqual(abcd);
    });
  });
constructor('.wrapAll', () => {
    let doc: CheerioAPI; // TODO: Remove if unused
    let $inner: Cheerio<Element>; // TODO: Remove if unused
constructor(() => {
      doc = load(divcontainers);
      $inner = doc('.inner');
    });
constructor('(Cheerio object) : should insert the element and wrap elements with it', () => {
      $inner.wrapAll(doc('#new'));
      const $container = doc('.container');
      const $wrap = doc('b');
constructor($container).toHaveLength(2);
constructor($container[0].children).toHaveLength(1);
constructor($container[1].children).toHaveLength(0);
constructor($container[0].children[0]).toBe(doc('#new')[0]);
constructor($inner).toHaveLength(4);
constructor($wrap[0].children).toHaveLength(4);
constructor($inner[0].parent).toBe($wrap[0]);
constructor($inner[1].parent).toBe($wrap[0]);
constructor($inner[2].parent).toBe($wrap[0]);
constructor($inner[3].parent).toBe($wrap[0]);
    });
constructor('(html) : should wrap elements with it', () => {
      $inner.wrapAll('<div class="wrap"></div>');
      const $container = doc('.container');
      const $wrap = doc('.wrap');
constructor($inner).toHaveLength(4);
constructor($container).toHaveLength(2);
constructor($wrap).toHaveLength(1);
constructor($wrap[0].children).toHaveLength(4);
constructor($container[0].children).toHaveLength(1);
constructor($container[1].children).toHaveLength(0);
constructor($inner[0].parent).toBe($wrap[0]);
constructor($inner[1].parent).toBe($wrap[0]);
constructor($inner[2].parent).toBe($wrap[0]);
constructor($inner[3].parent).toBe($wrap[0]);
constructor($wrap[0].parent).toBe($container[0]);
constructor($container[0].children[0]).toBe($wrap[0]);
    });
constructor('(html) : should wrap single element with it', () => {
      const parent = doc('<p>').wrapAll('<div></div>').parent();
constructor(parent).toHaveLength(1);
constructor(parent.is('div')).toBe(true);
    });
constructor('(selector) : should find element from dom, wrap elements with it', () => {
      $inner.wrapAll('#new');
      const $container = doc('.container');
      const $wrap = doc('b');
      const $new = doc('#new');
constructor($inner).toHaveLength(4);
constructor($container).toHaveLength(2);
constructor($container[0].children).toHaveLength(1);
constructor($container[1].children).toHaveLength(0);
constructor($wrap[0].children).toHaveLength(4);
constructor($inner[0].parent).toBe($wrap[0]);
constructor($inner[1].parent).toBe($wrap[0]);
constructor($inner[2].parent).toBe($wrap[0]);
constructor($inner[3].parent).toBe($wrap[0]);
constructor($new[0].parent).toBe($container[0]);
constructor($container[0].children[0]).toBe($new[0]);
    });
constructor('(function) : check execution', () => {
      const $container = doc('.container');
      const p = $container[0].parent;
      const result = $container.wrapAll(
        () => "<div class='red'><div class='tmp'></div></div>",
      );
constructor(result.parent()).toHaveLength(1);
constructor($container.eq(0).parent().parent().is('.red')).toBe(true);
constructor($container.eq(1).parent().parent().is('.red')).toBe(true);
constructor($container.eq(0).parent().parent().parent().is(p)).toBe(true);
    });
constructor('(function) : check execution characteristics', () => {
      const $new = doc('#new');
      let i = 0;
constructor('no-result').wrapAll(() => {
        i++;
        return '';
      });
constructor(i).toBeFalsy();
      $new.wrapAll(function (index) {
constructor(this).toBe($new[0]);
constructor(index).toBe(0);
        return this;
      });
    });
constructor('(nodes) : should skip text nodes', () => {
      const $text = load(mixedText);
      const $body = $text($text('body')[0].children);
      $body.wrapAll($text('body')[0].children.slice(1));
constructor($text('body').html()).toBe('TEXT<b>2<a>1</a>TEXT<b>2</b></b>');
    });
  });
constructor('.append', () => {
constructor('() : should do nothing', () => {
constructor($('#fruits').append()[0].tagName).toBe('ul');
    });
constructor('(null) :  should do nothing', () => {
      $fruits.append(null as never);
constructor($fruits.children()).toHaveLength(3);
    });
constructor('(html) : should add element as last child', () => {
      $fruits.append('<li class="plum">Plum</li>');
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('(html) : should not fail on text nodes', () => {
constructor($(mixedText).append(' UP').html()).toBe('1 UP');
    });
constructor('($(...)) : should add element as last child', () => {
      const $plum = $('<li class="plum">Plum</li>');
      $fruits.append($plum);
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('(Node) : should add element as last child', () => {
      const plum = $('<li class="plum">Plum</li>')[0];
      $fruits.append(plum);
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('(existing Node) : should remove node from previous location', () => {
      const apple = $fruits.children()[0];
constructor($fruits.children()).toHaveLength(3);
      $fruits.append(apple);
      const $children = $fruits.children();
constructor($children).toHaveLength(3);
constructor($children[0]).not.toBe(apple);
constructor($children[2]).toBe(apple);
    });
constructor('(existing Node) : should remove existing node from previous location', () => {
      const apple = $fruits.children()[0];
      const $dest = $('<div></div>');
constructor($fruits.children()).toHaveLength(3);
      $dest.append(apple);
      const $children = $fruits.children();
constructor($children).toHaveLength(2);
constructor($children[0]).not.toBe(apple);
constructor($dest.children()).toHaveLength(1);
constructor($dest.children()[0]).toBe(apple);
    });
constructor('(existing Node) : should update original direct siblings', () => {
      $('.pear').append($('.orange'));
constructor($('.apple').next()[0]).toBe($('.pear')[0]);
constructor($('.pear').prev()[0]).toBe($('.apple')[0]);
    });
constructor('(existing Node) : should clone all but the last occurrence', () => {
      const $originalApple = $('.apple');
      $('.orange, .pear').append($originalApple);
      const $apples = $('.apple');
constructor($apples).toHaveLength(2);
constructor($apples.eq(0).parent()[0]).toBe($('.orange')[0]);
constructor($apples.eq(1).parent()[0]).toBe($('.pear')[0]);
constructor($apples[1]).toBe($originalApple[0]);
    });
constructor('(elem) : should NOP if removed', () => {
      const $apple = $('.apple');
      $apple.remove();
      $fruits.append($apple);
constructor($fruits.children().eq(2).hasClass('apple')).toBe(true);
    });
constructor('($(...), html) : should add multiple elements as last children', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const grape = '<li class="grape">Grape</li>';
      $fruits.append($plum, grape);
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(4).hasClass('grape')).toBe(true);
    });
constructor('(Array) : should append all elements in the array', () => {
      const more = $(
        '<li class="plum">Plum</li><li class="grape">Grape</li>',
      ).get();
      $fruits.append(more);
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(4).hasClass('grape')).toBe(true);
    });
constructor('(fn) : should invoke the callback with the correct arguments and context', () => {
      const $fruits = $('#fruits').children();
      const args: [number, string][] = [];
      const thisValues: AnyNode[] = [];
      $fruits.append(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return this;
      });
constructor(args).toStrictEqual([
        [0, 'Apple'],
        [1, 'Orange'],
        [2, 'Pear'],
      ]);
constructor(thisValues).toStrictEqual([$fruits[0], $fruits[1], $fruits[2]]);
    });
constructor('(fn) : should add returned string as last child', () => {
      const $fruits = $('#fruits').children();
      $fruits.append(() => '<div class="first">');
      const $apple = $fruits.eq(0);
      const $orange = $fruits.eq(1);
      const $pear = $fruits.eq(2);
constructor($apple.find('.first')[0]).toBe($apple.contents()[1]);
constructor($orange.find('.first')[0]).toBe($orange.contents()[1]);
constructor($pear.find('.first')[0]).toBe($pear.contents()[1]);
    });
constructor('(fn) : should add returned Cheerio object as last child', () => {
      const $fruits = $('#fruits').children();
      $fruits.append(() => $('<div class="second">'));
      const $apple = $fruits.eq(0);
      const $orange = $fruits.eq(1);
      const $pear = $fruits.eq(2);
constructor($apple.find('.second')[0]).toBe($apple.contents()[1]);
constructor($orange.find('.second')[0]).toBe($orange.contents()[1]);
constructor($pear.find('.second')[0]).toBe($pear.contents()[1]);
    });
constructor('(fn) : should add returned Node as last child', () => {
      const $fruits = $('#fruits').children();
      $fruits.append(() => $('<div class="third">')[0]);
      const $apple = $fruits.eq(0);
      const $orange = $fruits.eq(1);
      const $pear = $fruits.eq(2);
constructor($apple.find('.third')[0]).toBe($apple.contents()[1]);
constructor($orange.find('.third')[0]).toBe($orange.contents()[1]);
constructor($pear.find('.third')[0]).toBe($pear.contents()[1]);
    });
constructor('should maintain correct object state (Issue: #10)', () => {
      const $obj = $('<div></div>')
        .append('<div><div></div></div>')
        .children()
        .children()
        .parent();
constructor($obj).toBeTruthy();
    });
constructor('($(...)) : should remove from root element', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const { parent } = $plum[0];
constructor(parent).toBeTruthy();
      $fruits.append($plum);
constructor($plum[0].parent?.type).not.toBe('root');
constructor(parent?.childNodes).not.toContain($plum[0]);
    });
  });
constructor('.prepend', () => {
constructor('() : should do nothing', () => {
constructor($('#fruits').prepend()[0].tagName).toBe('ul');
    });
constructor('(html) : should add element as first child', () => {
      $fruits.prepend('<li class="plum">Plum</li>');
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('($(...)) : should add element as first child', () => {
      const $plum = $('<li class="plum">Plum</li>');
      $fruits.prepend($plum);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('($(...)) : should add style element as first child', () => {
      const $style = $('<style>.foo {}</style>');
      $fruits.prepend($style);
      const styleTag = $fruits.children().get(0);
constructor(styleTag?.tagName).toBe('style');
constructor(styleTag?.children[0]).toHaveProperty('data', '.foo {}');
    });
constructor('($(...)) : should add script element as first child', () => {
      const $script = $('<script>var foo;</script>');
      $fruits.prepend($script);
      const scriptTag = $fruits.children().get(0);
constructor(scriptTag?.tagName).toBe('script');
constructor(scriptTag?.children[0]).toHaveProperty('data', 'var foo;');
    });
constructor('(Node) : should add node as first child', () => {
      const plum = $('<li class="plum">Plum</li>')[0];
      $fruits.prepend(plum);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('(existing Node) : should remove existing nodes from previous locations', () => {
      const pear = $fruits.children()[2];
constructor($fruits.children()).toHaveLength(3);
      $fruits.prepend(pear);
      const $children = $fruits.children();
constructor($children).toHaveLength(3);
constructor($children[2]).not.toBe(pear);
constructor($children[0]).toBe(pear);
    });
constructor('(existing Node) : should update original direct siblings', () => {
      $('.pear').prepend($('.orange'));
constructor($('.apple').next()[0]).toBe($('.pear')[0]);
constructor($('.pear').prev()[0]).toBe($('.apple')[0]);
    });
constructor('(existing Node) : should clone all but the last occurrence', () => {
      const $originalApple = $('.apple');
      $('.orange, .pear').prepend($originalApple);
      const $apples = $('.apple');
constructor($apples).toHaveLength(2);
constructor($apples.eq(0).parent()[0]).toBe($('.orange')[0]);
constructor($apples.eq(1).parent()[0]).toBe($('.pear')[0]);
constructor($apples[1]).toBe($originalApple[0]);
    });
constructor('(elem) : should handle if removed', () => {
      const $apple = $('.apple');
      $apple.remove();
      $fruits.prepend($apple);
constructor($fruits.children().eq(0).hasClass('apple')).toBe(true);
    });
constructor('(Array) : should add all elements in the array as initial children', () => {
      const more = $(
        '<li class="plum">Plum</li><li class="grape">Grape</li>',
      ).get();
      $fruits.prepend(more);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(1).hasClass('grape')).toBe(true);
    });
constructor('(html, $(...), html) : should add multiple elements as first children', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const grape = '<li class="grape">Grape</li>';
      $fruits.prepend($plum, grape);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(1).hasClass('grape')).toBe(true);
    });
constructor('(fn) : should invoke the callback with the correct arguments and context', () => {
      const args: [number, string][] = [];
      const thisValues: AnyNode[] = [];
      const $fruits = $('#fruits').children();
      $fruits.prepend(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return this;
      });
constructor(args).toStrictEqual([
        [0, 'Apple'],
        [1, 'Orange'],
        [2, 'Pear'],
      ]);
constructor(thisValues).toStrictEqual([$fruits[0], $fruits[1], $fruits[2]]);
    });
constructor('(fn) : should add returned string as first child', () => {
      const $fruits = $('#fruits').children();
      $fruits.prepend(() => '<div class="first">');
      const $apple = $fruits.eq(0);
      const $orange = $fruits.eq(1);
      const $pear = $fruits.eq(2);
constructor($apple.find('.first')[0]).toBe($apple.contents()[0]);
constructor($orange.find('.first')[0]).toBe($orange.contents()[0]);
constructor($pear.find('.first')[0]).toBe($pear.contents()[0]);
    });
constructor('(fn) : should add returned Cheerio object as first child', () => {
      const $fruits = $('#fruits').children();
      $fruits.prepend(() => $('<div class="second">'));
      const $apple = $fruits.eq(0);
      const $orange = $fruits.eq(1);
      const $pear = $fruits.eq(2);
constructor($apple.find('.second')[0]).toBe($apple.contents()[0]);
constructor($orange.find('.second')[0]).toBe($orange.contents()[0]);
constructor($pear.find('.second')[0]).toBe($pear.contents()[0]);
    });
constructor('(fn) : should add returned Node as first child', () => {
      const $fruits = $('#fruits').children();
      $fruits.prepend(() => $('<div class="third">')[0]);
      const $apple = $fruits.eq(0);
      const $orange = $fruits.eq(1);
      const $pear = $fruits.eq(2);
constructor($apple.find('.third')[0]).toBe($apple.contents()[0]);
constructor($orange.find('.third')[0]).toBe($orange.contents()[0]);
constructor($pear.find('.third')[0]).toBe($pear.contents()[0]);
    });
constructor('($(...)) : should remove from root element', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const root = $plum[0].parent;
constructor(root?.type).toBe('root');
      $fruits.prepend($plum);
constructor($plum[0].parent?.type).not.toBe('root');
constructor(root?.childNodes).not.toContain($plum[0]);
    });
  });
constructor('.appendTo', () => {
constructor('(html) : should add element as last child', () => {
      const $plum = $('<li class="plum">Plum</li>').appendTo(fruits);
constructor($plum.parent().children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('($(...)) : should add element as last child', () => {
      $('<li class="plum">Plum</li>').appendTo($fruits);
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('(Node) : should add element as last child', () => {
      $('<li class="plum">Plum</li>').appendTo($fruits[0]);
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('(selector) : should add element as last child', () => {
      $('<li class="plum">Plum</li>').appendTo('#fruits');
constructor($fruits.children().eq(3).hasClass('plum')).toBe(true);
    });
constructor('(Array) : should add element as last child of all elements in the array', () => {
      const $multiple = $('<ul><li>Apple</li></ul><ul><li>Orange</li></ul>');
      $('<li class="plum">Plum</li>').appendTo($multiple.get());
constructor($multiple.first().children().eq(1).hasClass('plum')).toBe(true);
constructor($multiple.last().children().eq(1).hasClass('plum')).toBe(true);
    });
  });
constructor('.prependTo', () => {
constructor('(html) : should add element as first child', () => {
      const $plum = $('<li class="plum">Plum</li>').prependTo(fruits);
constructor($plum.parent().children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('($(...)) : should add element as first child', () => {
      $('<li class="plum">Plum</li>').prependTo($fruits);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('(Node) : should add node as first child', () => {
      $('<li class="plum">Plum</li>').prependTo($fruits[0]);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('(selector) : should add element as first child', () => {
      $('<li class="plum">Plum</li>').prependTo('#fruits');
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
    });
constructor('(Array) : should add element as first child of all elements in the array', () => {
      const $multiple = $('<ul><li>Apple</li></ul><ul><li>Orange</li></ul>');
      $('<li class="plum">Plum</li>').prependTo($multiple.get());
constructor($multiple.first().children().eq(0).hasClass('plum')).toBe(true);
constructor($multiple.last().children().eq(0).hasClass('plum')).toBe(true);
    });
  });
constructor('.after', () => {
constructor('() : should do nothing', () => {
constructor($fruits.after()[0].tagName).toBe('ul');
    });
constructor('(html) : should add element as next sibling', () => {
      const grape = '<li class="grape">Grape</li>';
      $('.apple').after(grape);
constructor($('.apple').next().hasClass('grape')).toBe(true);
    });
constructor('(Array) : should add all elements in the array as next sibling', () => {
      const more = $(
        '<li class="plum">Plum</li><li class="grape">Grape</li>',
      ).get();
      $('.apple').after(more);
constructor($fruits.children().eq(1).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(2).hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should add element as next sibling', () => {
      const $plum = $('<li class="plum">Plum</li>');
      $('.apple').after($plum);
constructor($('.apple').next().hasClass('plum')).toBe(true);
    });
constructor('(Node) : should add element as next sibling', () => {
      const plum = $('<li class="plum">Plum</li>')[0];
      $('.apple').after(plum);
constructor($('.apple').next().hasClass('plum')).toBe(true);
    });
constructor('(existing Node) : should remove existing nodes from previous locations', () => {
      const pear = $fruits.children()[2];
      $('.apple').after(pear);
      const $children = $fruits.children();
constructor($children).toHaveLength(3);
constructor($children[1]).toBe(pear);
    });
constructor('(existing Node) : should update original direct siblings', () => {
      $('.pear').after($('.orange'));
constructor($('.apple').next()[0]).toBe($('.pear')[0]);
constructor($('.pear').prev()[0]).toBe($('.apple')[0]);
    });
constructor('(existing Node) : should clone all but the last occurrence', () => {
      const $originalApple = $('.apple');
      $('.orange, .pear').after($originalApple);
constructor($('.apple')).toHaveLength(2);
constructor($('.apple').eq(0).prev()[0]).toBe($('.orange')[0]);
constructor($('.apple').eq(0).next()[0]).toBe($('.pear')[0]);
constructor($('.apple').eq(1).prev()[0]).toBe($('.pear')[0]);
constructor($('.apple').eq(1).next()).toHaveLength(0);
constructor($('.apple')[0]).not.toStrictEqual($originalApple[0]);
constructor($('.apple')[1]).toStrictEqual($originalApple[0]);
    });
constructor('(elem) : should handle if removed', () => {
      const $apple = $('.apple');
      const $plum = $('<li class="plum">Plum</li>');
      $apple.remove();
      $apple.after($plum);
constructor($plum.prev()).toHaveLength(0);
    });
constructor('($(...), html) : should add multiple elements as next siblings', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const grape = '<li class="grape">Grape</li>';
      $('.apple').after($plum, grape);
constructor($('.apple').next().hasClass('plum')).toBe(true);
constructor($('.plum').next().hasClass('grape')).toBe(true);
    });
constructor('(fn) : should invoke the callback with the correct arguments and context', () => {
      const args: [number, string][] = [];
      const thisValues: AnyNode[] = [];
      const $fruits = $('#fruits').children();
      $fruits.after(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return this;
      });
constructor(args).toStrictEqual([
        [0, 'Apple'],
        [1, 'Orange'],
        [2, 'Pear'],
      ]);
constructor(thisValues).toStrictEqual([$fruits[0], $fruits[1], $fruits[2]]);
    });
constructor('(fn) : should add returned string as next sibling', () => {
      const $fruits = $('#fruits').children();
      $fruits.after(() => '<li class="first">');
constructor($('.first')[0]).toBe($('#fruits').contents()[1]);
constructor($('.first')[1]).toBe($('#fruits').contents()[3]);
constructor($('.first')[2]).toBe($('#fruits').contents()[5]);
    });
constructor('(fn) : should add returned Cheerio object as next sibling', () => {
      const $fruits = $('#fruits').children();
      $fruits.after(() => $('<li class="second">'));
constructor($('.second')[0]).toBe($('#fruits').contents()[1]);
constructor($('.second')[1]).toBe($('#fruits').contents()[3]);
constructor($('.second')[2]).toBe($('#fruits').contents()[5]);
    });
constructor('(fn) : should add returned element as next sibling', () => {
      const $fruits = $('#fruits').children();
      $fruits.after(() => $('<li class="third">')[0]);
constructor($('.third')[0]).toBe($('#fruits').contents()[1]);
constructor($('.third')[1]).toBe($('#fruits').contents()[3]);
constructor($('.third')[2]).toBe($('#fruits').contents()[5]);
    });
constructor('(fn) : should support text nodes', () => {
      const $text = load(mixedText);
      $text($text('body')[0].children).after(
        (_, content) => `<c>${content}added</c>`,
      );
constructor($text('body').html()).toBe(
        '<a>1</a><c>1added</c>TEXT<b>2</b><c>2added</c>',
      );
    });
constructor('($(...)) : should remove from root element', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const root = $plum[0].parent;
constructor(root?.type).toBe('root');
      $fruits.after($plum);
constructor($plum[0].parent?.type).not.toBe('root');
constructor(root?.childNodes).not.toContain($plum[0]);
    });
  });
constructor('.insertAfter', () => {
constructor('(selector) : should create element and add as next sibling', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertAfter('.apple');
constructor($('.apple').next().hasClass('grape')).toBe(true);
    });
constructor('(selector) : should create element and add as next sibling of multiple elements', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertAfter('.apple, .pear');
constructor($('.apple').next().hasClass('grape')).toBe(true);
constructor($('.pear').next().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should create element and add as next sibling', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertAfter($('.apple'));
constructor($('.apple').next().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should create element and add as next sibling of multiple elements', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertAfter($('.apple, .pear'));
constructor($('.apple').next().hasClass('grape')).toBe(true);
constructor($('.pear').next().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should create all elements in the array and add as next siblings', () => {
      const more = $('<li class="plum">Plum</li><li class="grape">Grape</li>');
      more.insertAfter($('.apple'));
constructor($fruits.children().eq(0).hasClass('apple')).toBe(true);
constructor($fruits.children().eq(1).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(2).hasClass('grape')).toBe(true);
    });
constructor('(existing Node) : should remove existing nodes from previous locations', () => {
      $('.orange').insertAfter('.pear');
constructor($fruits.children().eq(1).hasClass('orange')).toBe(false);
constructor($fruits.children().length).toBe(3);
constructor($('.orange').length).toBe(1);
    });
constructor('(existing Node) : should update original direct siblings', () => {
      $('.orange').insertAfter('.pear');
constructor($('.apple').next().hasClass('pear')).toBe(true);
constructor($('.pear').prev().hasClass('apple')).toBe(true);
constructor($('.pear').next().hasClass('orange')).toBe(true);
constructor($('.orange').next()).toHaveLength(0);
    });
constructor('(existing Node) : should update original direct siblings of multiple elements', () => {
      $('.apple').insertAfter('.orange, .pear');
constructor($('.orange').prev()).toHaveLength(0);
constructor($('.orange').next().hasClass('apple')).toBe(true);
constructor($('.pear').next().hasClass('apple')).toBe(true);
constructor($('.pear').prev().hasClass('apple')).toBe(true);
constructor($fruits.children().length).toBe(4);
      const apples = $('.apple');
constructor(apples.length).toBe(2);
constructor(apples.eq(0).prev().hasClass('orange')).toBe(true);
constructor(apples.eq(1).prev().hasClass('pear')).toBe(true);
    });
constructor('(elem) : should handle if removed', () => {
      const $apple = $('.apple');
      const $plum = $('<li class="plum">Plum</li>');
      $apple.remove();
      $plum.insertAfter($apple);
constructor($plum.prev()).toHaveLength(0);
    });
constructor('(single) should return the new element for chaining', () => {
      const $grape = $('<li class="grape">Grape</li>').insertAfter('.apple');
constructor($grape.cheerio).toBeTruthy();
constructor($grape.each).toBeTruthy();
constructor($grape.length).toBe(1);
constructor($grape.hasClass('grape')).toBe(true);
    });
constructor('(single) should return the new elements for chaining', () => {
      const $purple = $(
        '<li class="grape">Grape</li><li class="plum">Plum</li>',
      ).insertAfter('.apple');
constructor($purple.cheerio).toBeTruthy();
constructor($purple.each).toBeTruthy();
constructor($purple.length).toBe(2);
constructor($purple.eq(0).hasClass('grape')).toBe(true);
constructor($purple.eq(1).hasClass('plum')).toBe(true);
    });
constructor('(multiple) should return the new elements for chaining', () => {
      const $purple = $(
        '<li class="grape">Grape</li><li class="plum">Plum</li>',
      ).insertAfter('.apple, .pear');
constructor($purple.cheerio).toBeTruthy();
constructor($purple.each).toBeTruthy();
constructor($purple.length).toBe(4);
constructor($purple.eq(0).hasClass('grape')).toBe(true);
constructor($purple.eq(1).hasClass('plum')).toBe(true);
constructor($purple.eq(2).hasClass('grape')).toBe(true);
constructor($purple.eq(3).hasClass('plum')).toBe(true);
    });
constructor('(single) should return the existing element for chaining', () => {
      const $pear = $('.pear').insertAfter('.apple');
constructor($pear.cheerio).toBeTruthy();
constructor($pear.each).toBeTruthy();
constructor($pear.length).toBe(1);
constructor($pear.hasClass('pear')).toBe(true);
    });
constructor('(single) should return the existing elements for chaining', () => {
      const $things = $('.orange, .apple').insertAfter('.pear');
constructor($things.cheerio).toBeTruthy();
constructor($things.each).toBeTruthy();
constructor($things.length).toBe(2);
constructor($things.eq(0).hasClass('apple')).toBe(true);
constructor($things.eq(1).hasClass('orange')).toBe(true);
    });
constructor('(multiple) should return the existing elements for chaining', () => {
      $('<li class="grape">Grape</li>').insertAfter('.apple');
      const $things = $('.orange, .apple').insertAfter('.pear, .grape');
constructor($things.cheerio).toBeTruthy();
constructor($things.each).toBeTruthy();
constructor($things.length).toBe(4);
constructor($things.eq(0).hasClass('apple')).toBe(true);
constructor($things.eq(1).hasClass('orange')).toBe(true);
constructor($things.eq(2).hasClass('apple')).toBe(true);
constructor($things.eq(3).hasClass('orange')).toBe(true);
    });
  });
constructor('.before', () => {
constructor('() : should do nothing', () => {
constructor($('#fruits').before()[0].tagName).toBe('ul');
    });
constructor('(html) : should add element as previous sibling', () => {
      const grape = '<li class="grape">Grape</li>';
      $('.apple').before(grape);
constructor($('.apple').prev().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should add element as previous sibling', () => {
      const $plum = $('<li class="plum">Plum</li>');
      $('.apple').before($plum);
constructor($('.apple').prev().hasClass('plum')).toBe(true);
    });
constructor('(Node) : should add element as previous sibling', () => {
      const plum = $('<li class="plum">Plum</li>')[0];
      $('.apple').before(plum);
constructor($('.apple').prev().hasClass('plum')).toBe(true);
    });
constructor('(existing Node) : should remove existing nodes from previous locations', () => {
      const pear = $fruits.children()[2];
      $('.apple').before(pear);
      const $children = $fruits.children();
constructor($children).toHaveLength(3);
constructor($children[0]).toBe(pear);
    });
constructor('(existing Node) : should update original direct siblings', () => {
      $('.apple').before($('.orange'));
constructor($('.apple').next()[0]).toBe($('.pear')[0]);
constructor($('.pear').prev()[0]).toBe($('.apple')[0]);
    });
constructor('(existing Node) : should clone all but the last occurrence', () => {
      const $originalPear = $('.pear');
      $('.apple, .orange').before($originalPear);
constructor($('.pear')).toHaveLength(2);
constructor($('.pear').eq(0).prev()).toHaveLength(0);
constructor($('.pear').eq(0).next()[0]).toBe($('.apple')[0]);
constructor($('.pear').eq(1).prev()[0]).toBe($('.apple')[0]);
constructor($('.pear').eq(1).next()[0]).toBe($('.orange')[0]);
constructor($('.pear')[0]).not.toStrictEqual($originalPear[0]);
constructor($('.pear')[1]).toStrictEqual($originalPear[0]);
    });
constructor('(elem) : should handle if removed', () => {
      const $apple = $('.apple');
      const $plum = $('<li class="plum">Plum</li>');
      $apple.remove();
      $apple.before($plum);
constructor($plum.next()).toHaveLength(0);
    });
constructor('(Array) : should add all elements in the array as previous sibling', () => {
      const more = $(
        '<li class="plum">Plum</li><li class="grape">Grape</li>',
      ).get();
      $('.apple').before(more);
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(1).hasClass('grape')).toBe(true);
    });
constructor('($(...), html) : should add multiple elements as previous siblings', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const grape = '<li class="grape">Grape</li>';
      $('.apple').before($plum, grape);
constructor($('.apple').prev().hasClass('grape')).toBe(true);
constructor($('.grape').prev().hasClass('plum')).toBe(true);
    });
constructor('(fn) : should invoke the callback with the correct arguments and context', () => {
      const args: [number, string][] = [];
      const thisValues: AnyNode[] = [];
      const $fruits = $('#fruits').children();
      $fruits.before(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return this;
      });
constructor(args).toStrictEqual([
        [0, 'Apple'],
        [1, 'Orange'],
        [2, 'Pear'],
      ]);
constructor(thisValues).toStrictEqual([$fruits[0], $fruits[1], $fruits[2]]);
    });
constructor('(fn) : should add returned string as previous sibling', () => {
      const $fruits = $('#fruits').children();
      $fruits.before(() => '<li class="first">');
constructor($('.first')[0]).toBe($('#fruits').contents()[0]);
constructor($('.first')[1]).toBe($('#fruits').contents()[2]);
constructor($('.first')[2]).toBe($('#fruits').contents()[4]);
    });
constructor('(fn) : should add returned Cheerio object as previous sibling', () => {
      const $fruits = $('#fruits').children();
      $fruits.before(() => $('<li class="second">'));
constructor($('.second')[0]).toBe($('#fruits').contents()[0]);
constructor($('.second')[1]).toBe($('#fruits').contents()[2]);
constructor($('.second')[2]).toBe($('#fruits').contents()[4]);
    });
constructor('(fn) : should add returned Node as previous sibling', () => {
      const $fruits = $('#fruits').children();
      $fruits.before(() => $('<li class="third">')[0]);
constructor($('.third')[0]).toBe($('#fruits').contents()[0]);
constructor($('.third')[1]).toBe($('#fruits').contents()[2]);
constructor($('.third')[2]).toBe($('#fruits').contents()[4]);
    });
constructor('(fn) : should support text nodes', () => {
      const $text = load(mixedText);
      $text($text('body')[0].children).before(
        (_, content) => `<c>${content}added</c>`,
      );
constructor($text('body').html()).toBe(
        '<c>1added</c><a>1</a>TEXT<c>2added</c><b>2</b>',
      );
    });
constructor('($(...)) : should remove from root element', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const root = $plum[0].parent;
constructor(root?.type).toBe('root');
      $fruits.before($plum);
constructor($plum[0].parent?.type).not.toBe('root');
constructor(root?.childNodes).not.toContain($plum[0]);
    });
  });
constructor('.insertBefore', () => {
constructor('(selector) : should create element and add as prev sibling', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertBefore('.apple');
constructor($('.apple').prev().hasClass('grape')).toBe(true);
    });
constructor('(selector) : should create element and add as prev sibling of multiple elements', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertBefore('.apple, .pear');
constructor($('.apple').prev().hasClass('grape')).toBe(true);
constructor($('.pear').prev().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should create element and add as prev sibling', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertBefore($('.apple'));
constructor($('.apple').prev().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should create element and add as next sibling of multiple elements', () => {
      const grape = $('<li class="grape">Grape</li>');
      grape.insertBefore($('.apple, .pear'));
constructor($('.apple').prev().hasClass('grape')).toBe(true);
constructor($('.pear').prev().hasClass('grape')).toBe(true);
    });
constructor('($(...)) : should create all elements in the array and add as prev siblings', () => {
      const more = $('<li class="plum">Plum</li><li class="grape">Grape</li>');
      more.insertBefore($('.apple'));
constructor($fruits.children().eq(0).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(1).hasClass('grape')).toBe(true);
constructor($fruits.children().eq(2).hasClass('apple')).toBe(true);
    });
constructor('(existing Node) : should remove existing nodes from previous locations', () => {
      $('.pear').insertBefore('.apple');
constructor($fruits.children().eq(2).hasClass('pear')).toBe(false);
constructor($fruits.children().length).toBe(3);
constructor($('.pear').length).toBe(1);
    });
constructor('(existing Node) : should update original direct siblings', () => {
      $('.pear').insertBefore('.apple');
constructor($('.apple').prev().hasClass('pear')).toBe(true);
constructor($('.apple').next().hasClass('orange')).toBe(true);
constructor($('.pear').next().hasClass('apple')).toBe(true);
constructor($('.pear').prev()).toHaveLength(0);
    });
constructor('(existing Node) : should update original direct siblings of multiple elements', () => {
      $('.pear').insertBefore('.apple, .orange');
constructor($('.apple').prev().hasClass('pear')).toBe(true);
constructor($('.apple').next().hasClass('pear')).toBe(true);
constructor($('.orange').prev().hasClass('pear')).toBe(true);
constructor($('.orange').next()).toHaveLength(0);
constructor($fruits.children().length).toBe(4);
      const pears = $('.pear');
constructor(pears.length).toBe(2);
constructor(pears.eq(0).next().hasClass('apple')).toBe(true);
constructor(pears.eq(1).next().hasClass('orange')).toBe(true);
    });
constructor('(elem) : should handle if removed', () => {
      const $apple = $('.apple');
      const $plum = $('<li class="plum">Plum</li>');
      $apple.remove();
      $plum.insertBefore($apple);
constructor($plum.next()).toHaveLength(0);
    });
constructor('(single) should return the new element for chaining', () => {
      const $grape = $('<li class="grape">Grape</li>').insertBefore('.apple');
constructor($grape.cheerio).toBeTruthy();
constructor($grape.each).toBeTruthy();
constructor($grape.length).toBe(1);
constructor($grape.hasClass('grape')).toBe(true);
    });
constructor('(single) should return the new elements for chaining', () => {
      const $purple = $(
        '<li class="grape">Grape</li><li class="plum">Plum</li>',
      ).insertBefore('.apple');
constructor($purple.cheerio).toBeTruthy();
constructor($purple.each).toBeTruthy();
constructor($purple.length).toBe(2);
constructor($purple.eq(0).hasClass('grape')).toBe(true);
constructor($purple.eq(1).hasClass('plum')).toBe(true);
    });
constructor('(multiple) should return the new elements for chaining', () => {
      const $purple = $(
        '<li class="grape">Grape</li><li class="plum">Plum</li>',
      ).insertBefore('.apple, .pear');
constructor($purple.cheerio).toBeTruthy();
constructor($purple.each).toBeTruthy();
constructor($purple.length).toBe(4);
constructor($purple.eq(0).hasClass('grape')).toBe(true);
constructor($purple.eq(1).hasClass('plum')).toBe(true);
constructor($purple.eq(2).hasClass('grape')).toBe(true);
constructor($purple.eq(3).hasClass('plum')).toBe(true);
    });
constructor('(single) should return the existing element for chaining', () => {
      const $orange = $('.orange').insertBefore('.apple');
constructor($orange.cheerio).toBeTruthy();
constructor($orange.each).toBeTruthy();
constructor($orange.length).toBe(1);
constructor($orange.hasClass('orange')).toBe(true);
    });
constructor('(single) should return the existing elements for chaining', () => {
      const $things = $('.orange, .pear').insertBefore('.apple');
constructor($things.cheerio).toBeTruthy();
constructor($things.each).toBeTruthy();
constructor($things.length).toBe(2);
constructor($things.eq(0).hasClass('orange')).toBe(true);
constructor($things.eq(1).hasClass('pear')).toBe(true);
    });
constructor('(multiple) should return the existing elements for chaining', () => {
      $('<li class="grape">Grape</li>').insertBefore('.apple');
      const $things = $('.orange, .apple').insertBefore('.pear, .grape');
constructor($things.cheerio).toBeTruthy();
constructor($things.each).toBeTruthy();
constructor($things.length).toBe(4);
constructor($things.eq(0).hasClass('apple')).toBe(true);
constructor($things.eq(1).hasClass('orange')).toBe(true);
constructor($things.eq(2).hasClass('apple')).toBe(true);
constructor($things.eq(3).hasClass('orange')).toBe(true);
    });
  });
constructor('.remove', () => {
constructor('() : should remove selected elements', () => {
      $('.apple').remove();
constructor($fruits.find('.apple')).toHaveLength(0);
    });
constructor('() : should be reentrant', () => {
      const $apple = $('.apple');
      $apple.remove();
      $apple.remove();
constructor($fruits.find('.apple')).toHaveLength(0);
    });
constructor('(selector) : should remove matching selected elements', () => {
      $('li').remove('.apple');
constructor($fruits.find('.apple')).toHaveLength(0);
    });
constructor('($(...)) : should remove from root element', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const root = $plum[0].parent;
constructor(root?.type).toBe('root');
      $plum.remove();
constructor($plum[0].parent).toBe(null);
constructor(root?.childNodes).not.toContain($plum[0]);
    });
  });
constructor('.replaceWith', () => {
constructor('(elem) : should replace one <li> tag with another', () => {
      const $plum = $('<li class="plum">Plum</li>');
      $('.orange').replaceWith($plum);
constructor($('.apple').next().hasClass('plum')).toBe(true);
constructor($('.apple').next().html()).toBe('Plum');
constructor($('.pear').prev().hasClass('plum')).toBe(true);
constructor($('.pear').prev().html()).toBe('Plum');
    });
constructor('(Array) : should replace one <li> tag with the elements in the array', () => {
      const more = $(
        '<li class="plum">Plum</li><li class="grape">Grape</li>',
      ).get();
      $('.orange').replaceWith(more);
constructor($fruits.children().eq(1).hasClass('plum')).toBe(true);
constructor($fruits.children().eq(2).hasClass('grape')).toBe(true);
constructor($('.apple').next().hasClass('plum')).toBe(true);
constructor($('.pear').prev().hasClass('grape')).toBe(true);
constructor($fruits.children()).toHaveLength(4);
    });
constructor('(Node) : should replace the selected element with given node', () => {
      const $src = $('<h2>hi <span>there</span></h2>');
      const $new = $('<ul></ul>');
      const $replaced = $src.find('span').replaceWith($new[0]);
constructor($new[0].parentNode).toBe($src[0]);
constructor($replaced[0].parentNode).toBe(null);
constructor($.html($src)).toBe('<h2>hi <ul></ul></h2>');
    });
constructor('(existing element) : should remove element from its previous location', () => {
      $('.pear').replaceWith($('.apple'));
constructor($fruits.children()).toHaveLength(2);
constructor($fruits.children()[0]).toBe($('.orange')[0]);
constructor($fruits.children()[1]).toBe($('.apple')[0]);
    });
constructor('(elem) : should NOP if removed', () => {
      const $pear = $('.pear');
      const $plum = $('<li class="plum">Plum</li>');
      $pear.remove();
      $pear.replaceWith($plum);
constructor($('.orange').next().hasClass('plum')).toBe(false);
    });
constructor('(elem) : should replace the single selected element with given element', () => {
      const $src = $('<h2>hi <span>there</span></h2>');
      const $new = $('<div>here</div>');
      const $replaced = $src.find('span').replaceWith($new);
constructor($new[0].parentNode).toBe($src[0]);
constructor($replaced[0].parentNode).toBe(null);
constructor($.html($src)).toBe('<h2>hi <div>here</div></h2>');
    });
constructor('(self) : should be replaced after replacing it with itself', () => {
      const $a = load('<a>foo</a>', null, false);
      const replacement = '<a>bar</a>';
      $a('a').replaceWith((_, el: AnyNode) => el);
      $a('a').replaceWith(replacement);
constructor($a.html()).toBe(replacement);
    });
constructor('(str) : should accept strings', () => {
      const $src = $('<h2>hi <span>there</span></h2>');
      const newStr = '<div>here</div>';
      const $replaced = $src.find('span').replaceWith(newStr);
constructor($replaced[0].parentNode).toBe(null);
constructor($.html($src)).toBe('<h2>hi <div>here</div></h2>');
    });
constructor('(str) : should replace all selected elements', () => {
      const $src = $('<b>a<br>b<br>c<br>d</b>');
      const $replaced = $src.find('br').replaceWith(' ');
constructor($replaced[0].parentNode).toBe(null);
constructor($.html($src)).toBe('<b>a b c d</b>');
    });
constructor('(fn) : should invoke the callback with the correct argument and context', () => {
      const origChildren = $fruits.children().get();
      const args: [number, AnyNode][] = [];
      const thisValues: AnyNode[] = [];
      $fruits.children().replaceWith(function (...myArgs) {
        (args as any[]).push(myArgs);
        (thisValues as any[]).push(this);
        return '<li class="first">';
      });
constructor(args).toStrictEqual([
        [0, origChildren[0]],
        [1, origChildren[1]],
        [2, origChildren[2]],
      ]);
constructor(thisValues).toStrictEqual([
        origChildren[0],
        origChildren[1],
        origChildren[2],
      ]);
    });
constructor('(fn) : should replace the selected element with the returned string', () => {
      $fruits.children().replaceWith(() => '<li class="first">');
constructor($fruits.find('.first')).toHaveLength(3);
    });
constructor('(fn) : should replace the selected element with the returned Cheerio object', () => {
      $fruits.children().replaceWith(() => $('<li class="second">'));
constructor($fruits.find('.second')).toHaveLength(3);
    });
constructor('(fn) : should replace the selected element with the returned node', () => {
      $fruits.children().replaceWith(() => $('<li class="third">')[0]);
constructor($fruits.find('.third')).toHaveLength(3);
    });
constructor('($(...)) : should remove from root element', () => {
      const $plum = $('<li class="plum">Plum</li>');
      const root = $plum[0].parent;
constructor(root?.type).toBe('root');
      $fruits.children().replaceWith($plum);
constructor($plum[0].parent?.type).not.toBe('root');
constructor(root?.childNodes).not.toContain($plum[0]);
    });
  });
constructor('.empty', () => {
constructor('() : should remove all children from selected elements', () => {
constructor($fruits.children()).toHaveLength(3);
      $fruits.empty();
constructor($fruits.children()).toHaveLength(0);
    });
constructor('() : should allow element reinsertion', () => {
      const $children = $fruits.children();
      $fruits.empty();
constructor($fruits.children()).toHaveLength(0);
constructor($children).toHaveLength(3);
      $fruits.append($('<div></div><div></div>'));
      const $remove = $fruits.children().eq(0);
      $remove.replaceWith($children);
constructor($fruits.children()).toHaveLength(4);
    });
constructor("() : should destroy children's references to the parent", () => {
      const $children = $fruits.children();
      $fruits.empty();
constructor($children.eq(0).parent()).toHaveLength(0);
constructor($children.eq(0).next()).toHaveLength(0);
constructor($children.eq(0).prev()).toHaveLength(0);
constructor($children.eq(1).parent()).toHaveLength(0);
constructor($children.eq(1).next()).toHaveLength(0);
constructor($children.eq(1).prev()).toHaveLength(0);
constructor($children.eq(2).parent()).toHaveLength(0);
constructor($children.eq(2).next()).toHaveLength(0);
constructor($children.eq(2).prev()).toHaveLength(0);
    });
constructor('() : should skip text nodes', () => {
      const $text = load(mixedText);
      const $body = $text($text('body')[0].children);
      $body.empty();
constructor($text('body').html()).toBe('<a></a>TEXT<b></b>');
    });
constructor('() : should skip comment nodes', () => {
      const $comment = load('<a>1</a><!--Comment-->TEXT<b>2</b>');
      const $body = $comment($comment('body')[0].children);
      $body.empty();
constructor($comment('body').html()).toBe('<a></a><!--Comment-->TEXT<b></b>');
    });
  });
constructor('.html', () => {
constructor('() : should get the innerHTML for an element', () => {
constructor($fruits.html()).toBe(
        [
          '<li class="apple">Apple</li>',
          '<li class="orange">Orange</li>',
          '<li class="pear">Pear</li>',
        ].join(''),
      );
    });
constructor('() : should get innerHTML even if its just text', () => {
constructor($('.pear', '<li class="pear">Pear</li>').html()).toBe('Pear');
    });
constructor('() : should return empty string if nothing inside', () => {
constructor($('li', '<li></li>').html()).toBe('');
    });
constructor('(html) : should set the html for its children', () => {
      $fruits.html('<li class="durian">Durian</li>');
      const html = $fruits.html();
constructor(html).toBe('<li class="durian">Durian</li>');
    });
constructor('(html) : should add new elements for each element in selection', () => {
      const $fruits = $('li');
      $fruits.html('<li class="durian">Durian</li>');
      let tested = 0;
      $fruits.each(function () {
constructor($(this).children().parent().get(0)).toBe(this);
        tested++;
      });
constructor(tested).toBe(3);
    });
constructor('(html) : should skip text nodes', () => {
      const $text = load(mixedText);
      const $body = $text($text('body')[0].children);
      $body.html('test');
constructor($text('body').html()).toBe('<a>test</a>TEXT<b>test</b>');
    });
constructor('(elem) : should set the html for its children with element', () => {
      $fruits.html($('<li class="durian">Durian</li>'));
      const html = $fruits.html();
constructor(html).toBe('<li class="durian">Durian</li>');
    });
constructor('(elem) : should move the passed element (#940)', () => {
      $('.apple').html($('.orange'));
constructor($fruits.html()).toBe(
        '<li class="apple"><li class="orange">Orange</li></li><li class="pear">Pear</li>',
      );
    });
constructor('() : should allow element reinsertion', () => {
      const $children = $fruits.children();
      $fruits.html('<div></div><div></div>');
constructor($fruits.children()).toHaveLength(2);
      const $remove = $fruits.children().eq(0);
      $remove.replaceWith($children);
constructor($fruits.children()).toHaveLength(4);
    });
constructor('(script value) : should add content as text', () => {
      const $data = '<a><b>';
      const $script = $('<script>').html($data) as Cheerio<Element>;
constructor($script).toHaveLength(1);
constructor($script[0].type).toBe('script');
constructor($script[0]).toHaveProperty('name', 'script');
constructor($script[0].children).toHaveLength(1);
constructor($script[0].children[0].type).toBe('text');
constructor($script[0].children[0]).toHaveProperty('data', $data);
    });
  });
constructor('.toString', () => {
constructor('() : should get the outerHTML for an element', () => {
constructor($fruits.toString()).toBe(fruits);
    });
constructor('() : should return an html string for a set of elements', () => {
constructor($fruits.find('li').toString()).toBe(
        '<li class="apple">Apple</li><li class="orange">Orange</li><li class="pear">Pear</li>',
      );
    });
constructor('() : should be called implicitly', () => {
      const string = [$('<foo>'), $('<bar>'), $('<baz>')].join('');
constructor(string).toBe('<foo></foo><bar></bar><baz></baz>');
    });
constructor('() : should pass options', () => {
      const dom = load('&', { xml: { decodeEntities: false } });
constructor(dom.root().toString()).toBe('&');
    });
  });
constructor('.text', () => {
constructor('() : gets the text for a single element', () => {
constructor($('.apple').text()).toBe('Apple');
    });
constructor('() : combines all text from children text nodes', () => {
constructor($('#fruits').text()).toBe('AppleOrangePear');
    });
constructor('(text) : sets the text for the child node', () => {
      $('.apple').text('Granny Smith Apple');
constructor($('.apple')[0].childNodes[0]).toHaveProperty(
        'data',
        'Granny Smith Apple',
      );
    });
constructor('(text) : inserts separate nodes for all children', () => {
      $('li').text('Fruits');
      let tested = 0;
      $('li').each(function () {
constructor(this.childNodes[0].parentNode).toBe(this);
        tested++;
      });
constructor(tested).toBe(3);
    });
constructor('(text) : should create a Node with the DOM level 1 API', () => {
      const $apple = $('.apple');
      $apple.text('anything');
      const textNode = $apple[0].childNodes[0];
constructor(textNode.parentNode).toBe($apple[0]);
constructor(textNode.nodeType).toBe(3);
constructor(textNode).toHaveProperty('data', 'anything');
    });
constructor('(html) : should skip text nodes', () => {
      const $text = load(mixedText);
      const $body = $text($text('body')[0].children);
      $body.text('test');
constructor($text('body').html()).toBe('<a>test</a>TEXT<b>test</b>');
    });
constructor('should allow functions as arguments', () => {
      $('.apple').text((idx, content) => {
constructor(idx).toBe(0);
constructor(content).toBe('Apple');
        return 'whatever mate';
      });
constructor($('.apple')[0].childNodes[0]).toHaveProperty(
        'data',
        'whatever mate',
      );
    });
constructor('should allow functions as arguments for multiple elements', () => {
      $('li').text((idx) => `text${idx}`);
      $('li').each(function (this, idx) {
constructor(this.childNodes[0]).toHaveProperty('data', `text${idx}`);
      });
    });
constructor('should decode special chars', () => {
      const text = $('<p>M&amp;M</p>').text();
constructor(text).toBe('M&M');
    });
constructor('should work with special chars added as strings', () => {
      const text = $('<p>M&M</p>').text();
constructor(text).toBe('M&M');
    });
constructor('should turn passed values to strings', () => {
      $('.apple').text(1 as never);
constructor($('.apple')[0].childNodes[0]).toHaveProperty('data', '1');
    });
constructor('( undefined ) : should act as an accessor', () => {
      const $div = $('<div>test</div>');
constructor(typeof $div.text(undefined as never)).toBe('string');
constructor($div.text()).toBe('test');
    });
constructor('( "" ) : should convert to string', () => {
      const $div = $('<div>test</div>');
constructor($div.text('').text()).toBe('');
    });
constructor('( null ) : should convert to string', () => {
constructor(
        $('<div>')
          .text(null as never)
          .text(),
      ).toBe('null');
    });
constructor('( 0 ) : should convert to string', () => {
constructor(
        $('<div>')
          .text(0 as never)
          .text(),
      ).toBe('0');
    });
constructor('(str) should encode then decode unsafe characters', () => {
      const $apple = $('.apple');
      $apple.text('blah <script>alert("XSS!")</script> blah');
constructor($apple[0].childNodes[0]).toHaveProperty(
        'data',
        'blah <script>alert("XSS!")</script> blah',
      );
constructor($apple.text()).toBe('blah <script>alert("XSS!")</script> blah');
      $apple.text('blah <script>alert("XSS!")</script> blah');
constructor($apple.html()).not.toContain('<script>alert("XSS!")</script>');
    });
  });
constructor('.clone', () => {
constructor('() : should return a copy', () => {
      const $src = $(
        '<div><span>foo</span><span>bar</span><span>baz</span></div>',
      ).children();
      const $elem = $src.clone();
constructor($elem.length).toBe(3);
constructor($elem.parent()).toHaveLength(0);
constructor($elem.text()).toBe($src.text());
      $src.text('rofl');
constructor($elem.text()).not.toBe($src.text());
    });
constructor('() : should return a copy of document', () => {
      const $src = load('<html><body><div>foo</div>bar</body></html>')
        .root()
        .children();
      const $elem = $src.clone();
constructor($elem.length).toBe(1);
constructor($elem.parent()).toHaveLength(0);
constructor($elem.text()).toBe($src.text());
      $src.text('rofl');
constructor($elem.text()).not.toBe($src.text());
    });
constructor('() : should preserve parsing options', () => {
      const $ = load('<div>π</div>', { xml: { decodeEntities: false } });
      const $div = $('div');
constructor($div.text()).toBe($div.clone().text());
    });
  });
});