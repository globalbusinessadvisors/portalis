import process from "node:process";
/**
 * The `os` module provides operating system-related utility methods and
 * properties. It can be accessed using:
 *
 * ```js
 * import os from 'node:os';
 * ```
 * @see [source](https://github.com/nodejs/node/blob/v18.0.0/lib/os.js)
 */
declare module "os" {
    interface CpuInfo {
        model: string;
        speed: number;
        times: {
            user: number;
            nice: number;
            sys: number;
            idle: number;
            irq: number;
        };
    }
    interface NetworkInterfaceBase {
        address: string;
        netmask: string;
        mac: string;
        internal: boolean;
        cidr: string | null;
    }
    interface NetworkInterfaceInfoIPv4 extends NetworkInterfaceBase {
        family: "IPv4";
        scopeid?: undefined;
    }
    interface NetworkInterfaceInfoIPv6 extends NetworkInterfaceBase {
        family: "IPv6";
        scopeid: number;
    }
    interface UserInfo<T> {
        username: T;
        uid: number;
        gid: number;
        shell: T | null;
        homedir: T;
    }
    type NetworkInterfaceInfo = NetworkInterfaceInfoIPv4 | NetworkInterfaceInfoIPv6;
    /**
     * Returns the host name of the operating system as a string.
     * @since v0.3.3
     */
    function hostname(): string;
    /**
     * Returns an array containing the 1, 5, and 15 minute load averages.
     *
     * The load average is a measure of system activity calculated by the operating
     * system and expressed as a fractional number.
     *
     * The load average is a Unix-specific concept. On Windows, the return value is
     * always `[0, 0, 0]`.
     * @since v0.3.3
     */
    function loadavg(): number[];
    /**
     * Returns the system uptime in number of seconds.
     * @since v0.3.3
     */
    function uptime(): number;
    /**
     * Returns the amount of free system memory in bytes as an integer.
     * @since v0.3.3
     */
    function freemem(): number;
    /**
     * Returns the total amount of system memory in bytes as an integer.
     * @since v0.3.3
     */
    function totalmem(): number;
    /**
     * Returns an array of objects containing information about each logical CPU core.
     *
     * The properties included on each object include:
     *
     * ```js
     * [
     *   {
     *     model: 'Intel(R) Core(TM) i7 CPU         860  @ 2.80GHz',
     *     speed: 2926,
     *     times: {
     *       user: 252020,
     *       nice: 0,
     *       sys: 30340,
     *       idle: 1070356870,
     *       irq: 0
     *     }
     *   },
     *   {
     *     model: 'Intel(R) Core(TM) i7 CPU         860  @ 2.80GHz',
     *     speed: 2926,
     *     times: {
     *       user: 306960,
     *       nice: 0,
     *       sys: 26980,
     *       idle: 1071569080,
     *       irq: 0
     *     }
     *   },
     *   {
     *     model: 'Intel(R) Core(TM) i7 CPU         860  @ 2.80GHz',
     *     speed: 2926,
     *     times: {
     *       user: 248450,
     *       nice: 0,
     *       sys: 21750,
     *       idle: 1070919370,
     *       irq: 0
     *     }
     *   },
     *   {
     *     model: 'Intel(R) Core(TM) i7 CPU         860  @ 2.80GHz',
     *     speed: 2926,
     *     times: {
     *       user: 256880,
     *       nice: 0,
     *       sys: 19430,
     *       idle: 1070905480,
     *       irq: 20
     *     }
     *   },
     * ]
     * ```
     *
     * `nice` values are POSIX-only. On Windows, the `nice` values of all processors
     * are always 0.
     * @since v0.3.3
     */
    function cpus(): CpuInfo[];
    /**
     * Returns an estimate of the default amount of parallelism a program should use. Always returns a value greater than zero.
     *
     * This function is a small wrapper about libuv's [`uv_available_parallelism()`](https://docs.libuv.org/en/v1.x/misc.html#c.uv_available_parallelism).
     * @since 18.4.0
     */
    function availableParallelism(): number;
    /**
     * Returns the operating system name as returned by [`uname(3)`](https://linux.die.net/man/3/uname). For example, it
     * returns `'Linux'` on Linux, `'Darwin'` on macOS, and `'Windows_NT'` on Windows.
     *
     * See [https://en.wikipedia.org/wiki/Uname#Examples](https://en.wikipedia.org/wiki/Uname#Examples) for additional information
     * about the output of running [`uname(3)`](https://linux.die.net/man/3/uname) on various operating systems.
     * @since v0.3.3
     */
    function type(): string;
    /**
     * Returns the operating system as a string.
     *
     * On POSIX systems, the operating system release is determined by calling [`uname(3)`](https://linux.die.net/man/3/uname). On Windows, `GetVersionExW()` is used. See
     * [https://en.wikipedia.org/wiki/Uname#Examples](https://en.wikipedia.org/wiki/Uname#Examples) for more information.
     * @since v0.3.3
     */
    function release(): string;
    /**
     * Returns an object containing network interfaces that have been assigned a
     * network address.
     *
     * Each key on the returned object identifies a network interface. The associated
     * value is an array of objects that each describe an assigned network address.
     *
     * The properties available on the assigned network address object include:
     *
     * ```js
     * {
     *   lo: [
     *     {
     *       address: '127.0.0.1',
     *       netmask: '255.0.0.0',
     *       family: 'IPv4',
     *       mac: '00:00:00:00:00:00',
     *       internal: true,
     *       cidr: '127.0.0.1/8'
     *     },
     *     {
     *       address: '::1',
     *       netmask: 'ffff:ffff:ffff:ffff:ffff:ffff:ffff:ffff',
     *       family: 'IPv6',
     *       mac: '00:00:00:00:00:00',
     *       scopeid: 0,
     *       internal: true,
     *       cidr: '::1/128'
     *     }
     *   ],
     *   eth0: [
     *     {
     *       address: '192.168.1.108',
     *       netmask: '255.255.255.0',
     *       family: 'IPv4',
     *       mac: '01:02:03:0a:0b:0c',
     *       internal: false,
     *       cidr: '192.168.1.108/24'
     *     },
     *     {
     *       address: 'fe80::a00:27ff:fe4e:66a1',
     *       netmask: 'ffff:ffff:ffff:ffff::',
     *       family: 'IPv6',
     *       mac: '01:02:03:0a:0b:0c',
     *       scopeid: 1,
     *       internal: false,
     *       cidr: 'fe80::a00:27ff:fe4e:66a1/64'
     *     }
     *   ]
     * }
     * ```
     * @since v0.6.0
     */
    function networkInterfaces(): NodeJS.Dict<NetworkInterfaceInfo[]>;
    /**
     * Returns the string path of the current user's home directory.
     *
     * On POSIX, it uses the `$HOME` environment variable if defined. Otherwise it
     * uses the [effective UID](https://en.wikipedia.org/wiki/User_identifier#Effective_user_ID) to look up the user's home directory.
     *
     * On Windows, it uses the `USERPROFILE` environment variable if defined.
     * Otherwise it uses the path to the profile directory of the current user.
     * @since v2.3.0
     */
    function homedir(): string;
    /**
     * Returns information about the currently effective user. On POSIX platforms,
     * this is typically a subset of the password file. The returned object includes
     * the `username`, `uid`, `gid`, `shell`, and `homedir`. On Windows, the `uid` and `gid` fields are `-1`, and `shell` is `null`.
     *
     * The value of `homedir` returned by `os.userInfo()` is provided by the operating
     * system. This differs from the result of `os.homedir()`, which queries
     * environment variables for the home directory before falling back to the
     * operating system response.
     *
     * Throws a `SystemError` if a user has no `username` or `homedir`.
     * @since v6.0.0
     */
    function userInfo(options: { encoding: "buffer" }): UserInfo<Buffer>;
    function userInfo(options?: { encoding: BufferEncoding }): UserInfo<string>;
    type SignalConstants = {
        [key in NodeJS.Signals]: number;
    };
    namespace constants {
        const UV_UDP_REUSEADDR: number; // TODO: Remove if unused
        namespace signals {}
        const signals: SignalConstants; // TODO: Remove if unused
        namespace errno {
            const E2BIG: number; // TODO: Remove if unused
            const EACCES: number; // TODO: Remove if unused
            const EADDRINUSE: number; // TODO: Remove if unused
            const EADDRNOTAVAIL: number; // TODO: Remove if unused
            const EAFNOSUPPORT: number; // TODO: Remove if unused
            const EAGAIN: number; // TODO: Remove if unused
            const EALREADY: number; // TODO: Remove if unused
            const EBADF: number; // TODO: Remove if unused
            const EBADMSG: number; // TODO: Remove if unused
            const EBUSY: number; // TODO: Remove if unused
            const ECANCELED: number; // TODO: Remove if unused
            const ECHILD: number; // TODO: Remove if unused
            const ECONNABORTED: number; // TODO: Remove if unused
            const ECONNREFUSED: number; // TODO: Remove if unused
            const ECONNRESET: number; // TODO: Remove if unused
            const EDEADLK: number; // TODO: Remove if unused
            const EDESTADDRREQ: number; // TODO: Remove if unused
            const EDOM: number; // TODO: Remove if unused
            const EDQUOT: number; // TODO: Remove if unused
            const EEXIST: number; // TODO: Remove if unused
            const EFAULT: number; // TODO: Remove if unused
            const EFBIG: number; // TODO: Remove if unused
            const EHOSTUNREACH: number; // TODO: Remove if unused
            const EIDRM: number; // TODO: Remove if unused
            const EILSEQ: number; // TODO: Remove if unused
            const EINPROGRESS: number; // TODO: Remove if unused
            const EINTR: number; // TODO: Remove if unused
            const EINVAL: number; // TODO: Remove if unused
            const EIO: number; // TODO: Remove if unused
            const EISCONN: number; // TODO: Remove if unused
            const EISDIR: number; // TODO: Remove if unused
            const ELOOP: number; // TODO: Remove if unused
            const EMFILE: number; // TODO: Remove if unused
            const EMLINK: number; // TODO: Remove if unused
            const EMSGSIZE: number; // TODO: Remove if unused
            const EMULTIHOP: number; // TODO: Remove if unused
            const ENAMETOOLONG: number; // TODO: Remove if unused
            const ENETDOWN: number; // TODO: Remove if unused
            const ENETRESET: number; // TODO: Remove if unused
            const ENETUNREACH: number; // TODO: Remove if unused
            const ENFILE: number; // TODO: Remove if unused
            const ENOBUFS: number; // TODO: Remove if unused
            const ENODATA: number; // TODO: Remove if unused
            const ENODEV: number; // TODO: Remove if unused
            const ENOENT: number; // TODO: Remove if unused
            const ENOEXEC: number; // TODO: Remove if unused
            const ENOLCK: number; // TODO: Remove if unused
            const ENOLINK: number; // TODO: Remove if unused
            const ENOMEM: number; // TODO: Remove if unused
            const ENOMSG: number; // TODO: Remove if unused
            const ENOPROTOOPT: number; // TODO: Remove if unused
            const ENOSPC: number; // TODO: Remove if unused
            const ENOSR: number; // TODO: Remove if unused
            const ENOSTR: number; // TODO: Remove if unused
            const ENOSYS: number; // TODO: Remove if unused
            const ENOTCONN: number; // TODO: Remove if unused
            const ENOTDIR: number; // TODO: Remove if unused
            const ENOTEMPTY: number; // TODO: Remove if unused
            const ENOTSOCK: number; // TODO: Remove if unused
            const ENOTSUP: number; // TODO: Remove if unused
            const ENOTTY: number; // TODO: Remove if unused
            const ENXIO: number; // TODO: Remove if unused
            const EOPNOTSUPP: number; // TODO: Remove if unused
            const EOVERFLOW: number; // TODO: Remove if unused
            const EPERM: number; // TODO: Remove if unused
            const EPIPE: number; // TODO: Remove if unused
            const EPROTO: number; // TODO: Remove if unused
            const EPROTONOSUPPORT: number; // TODO: Remove if unused
            const EPROTOTYPE: number; // TODO: Remove if unused
            const ERANGE: number; // TODO: Remove if unused
            const EROFS: number; // TODO: Remove if unused
            const ESPIPE: number; // TODO: Remove if unused
            const ESRCH: number; // TODO: Remove if unused
            const ESTALE: number; // TODO: Remove if unused
            const ETIME: number; // TODO: Remove if unused
            const ETIMEDOUT: number; // TODO: Remove if unused
            const ETXTBSY: number; // TODO: Remove if unused
            const EWOULDBLOCK: number; // TODO: Remove if unused
            const EXDEV: number; // TODO: Remove if unused
            const WSAEINTR: number; // TODO: Remove if unused
            const WSAEBADF: number; // TODO: Remove if unused
            const WSAEACCES: number; // TODO: Remove if unused
            const WSAEFAULT: number; // TODO: Remove if unused
            const WSAEINVAL: number; // TODO: Remove if unused
            const WSAEMFILE: number; // TODO: Remove if unused
            const WSAEWOULDBLOCK: number; // TODO: Remove if unused
            const WSAEINPROGRESS: number; // TODO: Remove if unused
            const WSAEALREADY: number; // TODO: Remove if unused
            const WSAENOTSOCK: number; // TODO: Remove if unused
            const WSAEDESTADDRREQ: number; // TODO: Remove if unused
            const WSAEMSGSIZE: number; // TODO: Remove if unused
            const WSAEPROTOTYPE: number; // TODO: Remove if unused
            const WSAENOPROTOOPT: number; // TODO: Remove if unused
            const WSAEPROTONOSUPPORT: number; // TODO: Remove if unused
            const WSAESOCKTNOSUPPORT: number; // TODO: Remove if unused
            const WSAEOPNOTSUPP: number; // TODO: Remove if unused
            const WSAEPFNOSUPPORT: number; // TODO: Remove if unused
            const WSAEAFNOSUPPORT: number; // TODO: Remove if unused
            const WSAEADDRINUSE: number; // TODO: Remove if unused
            const WSAEADDRNOTAVAIL: number; // TODO: Remove if unused
            const WSAENETDOWN: number; // TODO: Remove if unused
            const WSAENETUNREACH: number; // TODO: Remove if unused
            const WSAENETRESET: number; // TODO: Remove if unused
            const WSAECONNABORTED: number; // TODO: Remove if unused
            const WSAECONNRESET: number; // TODO: Remove if unused
            const WSAENOBUFS: number; // TODO: Remove if unused
            const WSAEISCONN: number; // TODO: Remove if unused
            const WSAENOTCONN: number; // TODO: Remove if unused
            const WSAESHUTDOWN: number; // TODO: Remove if unused
            const WSAETOOMANYREFS: number; // TODO: Remove if unused
            const WSAETIMEDOUT: number; // TODO: Remove if unused
            const WSAECONNREFUSED: number; // TODO: Remove if unused
            const WSAELOOP: number; // TODO: Remove if unused
            const WSAENAMETOOLONG: number; // TODO: Remove if unused
            const WSAEHOSTDOWN: number; // TODO: Remove if unused
            const WSAEHOSTUNREACH: number; // TODO: Remove if unused
            const WSAENOTEMPTY: number; // TODO: Remove if unused
            const WSAEPROCLIM: number; // TODO: Remove if unused
            const WSAEUSERS: number; // TODO: Remove if unused
            const WSAEDQUOT: number; // TODO: Remove if unused
            const WSAESTALE: number; // TODO: Remove if unused
            const WSAEREMOTE: number; // TODO: Remove if unused
            const WSASYSNOTREADY: number; // TODO: Remove if unused
            const WSAVERNOTSUPPORTED: number; // TODO: Remove if unused
            const WSANOTINITIALISED: number; // TODO: Remove if unused
            const WSAEDISCON: number; // TODO: Remove if unused
            const WSAENOMORE: number; // TODO: Remove if unused
            const WSAECANCELLED: number; // TODO: Remove if unused
            const WSAEINVALIDPROCTABLE: number; // TODO: Remove if unused
            const WSAEINVALIDPROVIDER: number; // TODO: Remove if unused
            const WSAEPROVIDERFAILEDINIT: number; // TODO: Remove if unused
            const WSASYSCALLFAILURE: number; // TODO: Remove if unused
            const WSASERVICE_NOT_FOUND: number; // TODO: Remove if unused
            const WSATYPE_NOT_FOUND: number; // TODO: Remove if unused
            const WSA_E_NO_MORE: number; // TODO: Remove if unused
            const WSA_E_CANCELLED: number; // TODO: Remove if unused
            const WSAEREFUSED: number; // TODO: Remove if unused
        }
        namespace dlopen {
            const RTLD_LAZY: number; // TODO: Remove if unused
            const RTLD_NOW: number; // TODO: Remove if unused
            const RTLD_GLOBAL: number; // TODO: Remove if unused
            const RTLD_LOCAL: number; // TODO: Remove if unused
            const RTLD_DEEPBIND: number; // TODO: Remove if unused
        }
        namespace priority {
            const PRIORITY_LOW: number; // TODO: Remove if unused
            const PRIORITY_BELOW_NORMAL: number; // TODO: Remove if unused
            const PRIORITY_NORMAL: number; // TODO: Remove if unused
            const PRIORITY_ABOVE_NORMAL: number; // TODO: Remove if unused
            const PRIORITY_HIGH: number; // TODO: Remove if unused
            const PRIORITY_HIGHEST: number; // TODO: Remove if unused
        }
    }
    const devNull: string; // TODO: Remove if unused
    const EOL: string; // TODO: Remove if unused
    /**
     * Returns the operating system CPU architecture for which the Node.js binary was
     * compiled. Possible values are `'arm'`, `'arm64'`, `'ia32'`, `'mips'`, `'mipsel'`, `'ppc'`, `'ppc64'`, `'s390'`, `'s390x'`, and `'x64'`.
     *
     * The return value is equivalent to `process.arch`.
     * @since v0.5.0
     */
    function arch(): string;
    /**
     * Returns a string identifying the kernel version.
     *
     * On POSIX systems, the operating system release is determined by calling [`uname(3)`](https://linux.die.net/man/3/uname). On Windows, `RtlGetVersion()` is used, and if it is not
     * available, `GetVersionExW()` will be used. See [https://en.wikipedia.org/wiki/Uname#Examples](https://en.wikipedia.org/wiki/Uname#Examples) for more information.
     * @since v13.11.0, v12.17.0
     */
    function version(): string;
    /**
     * Returns a string identifying the operating system platform for which
     * the Node.js binary was compiled. The value is set at compile time.
     * Possible values are `'aix'`, `'darwin'`, `'freebsd'`, `'linux'`, `'openbsd'`, `'sunos'`, and `'win32'`.
     *
     * The return value is equivalent to `process.platform`.
     *
     * The value `'android'` may also be returned if Node.js is built on the Android
     * operating system. [Android support is experimental](https://github.com/nodejs/node/blob/HEAD/BUILDING.md#androidandroid-based-devices-eg-firefox-os).
     * @since v0.5.0
     */
    function platform(): NodeJS.Platform;
    /**
     * Returns the machine type as a string, such as arm, aarch64, mips, mips64, ppc64, ppc64le, s390, s390x, i386, i686, x86_64.
     *
     * On POSIX systems, the machine type is determined by calling [`uname(3)`](https://linux.die.net/man/3/uname).
     * On Windows, `RtlGetVersion()` is used, and if it is not available, `GetVersionExW()` will be used.
     * See [https://en.wikipedia.org/wiki/Uname#Examples](https://en.wikipedia.org/wiki/Uname#Examples) for more information.
     * @since v18.9.0
     */
    function machine(): string;
    /**
     * Returns the operating system's default directory for temporary files as a
     * string.
     * @since v0.9.9
     */
    function tmpdir(): string;
    /**
     * Returns a string identifying the endianness of the CPU for which the Node.js
     * binary was compiled.
     *
     * Possible values are `'BE'` for big endian and `'LE'` for little endian.
     * @since v0.9.4
     */
    function endianness(): "BE" | "LE";
    /**
     * Returns the scheduling priority for the process specified by `pid`. If `pid` is
     * not provided or is `0`, the priority of the current process is returned.
     * @since v10.10.0
     * @param [pid=0] The process ID to retrieve scheduling priority for.
     */
    function getPriority(pid?: number): number;
    /**
     * Attempts to set the scheduling priority for the process specified by `pid`. If`pid` is not provided or is `0`, the process ID of the current process is used.
     *
     * The `priority` input must be an integer between `-20` (high priority) and `19` (low priority). Due to differences between Unix priority levels and Windows
     * priority classes, `priority` is mapped to one of six priority constants in`os.constants.priority`. When retrieving a process priority level, this range
     * mapping may cause the return value to be slightly different on Windows. To avoid
     * confusion, set `priority` to one of the priority constants.
     *
     * On Windows, setting priority to `PRIORITY_HIGHEST` requires elevated user
     * privileges. Otherwise the set priority will be silently reduced to`PRIORITY_HIGH`.
     * @since v10.10.0
     * @param [pid=0] The process ID to set scheduling priority for.
     * @param priority The scheduling priority to assign to the process.
     */
    function setPriority(priority: number): void;
    function setPriority(pid: number, priority: number): void;
}
declare module "node:os" {
    export * from "os";
}