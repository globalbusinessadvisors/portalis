import semver = require("../index");
import Comparator = require("./comparator");
import SemVer = require("./semver");

declare class Range {
constructor(range: string | Range, optionsOrLoose?: boolean | semver.RangeOptions);

    range: string;
    raw: string;
    loose: boolean;
    options: semver.Options;
    includePrerelease: boolean;
constructor(): string;
constructor(): string;

    set: ReadonlyArray<readonly Comparator[]>;
constructor(range: string): readonly Comparator[];
constructor(version: string | SemVer): boolean;
constructor(range: Range, optionsOrLoose?: boolean | semver.Options): boolean;
}
export = Range;
