import process from "node:process";
declare const cliCursor: {
	/**
	Show cursor.

	@param stream - Default: `process.stderr`.

	@example
	```
	import cliCursor from 'cli-cursor';

	cliCursor.show();
	```
	*/
constructor(stream?: NodeJS.WritableStream): void;

	/**
	Hide cursor.

	@param stream - Default: `process.stderr`.

	@example
	```
	import cliCursor from 'cli-cursor';

	cliCursor.hide();
	```
	*/
constructor(stream?: NodeJS.WritableStream): void;

	/**
	Toggle cursor visibility.

	@param force - Is useful to show or hide the cursor based on a boolean.
	@param stream - Default: `process.stderr`.

	@example
	```
	import cliCursor from 'cli-cursor';

	const unicornsAreAwesome = true;
	cliCursor.toggle(unicornsAreAwesome);
	```
	*/
constructor(force?: boolean, stream?: NodeJS.WritableStream): void;
};

export default cliCursor;
