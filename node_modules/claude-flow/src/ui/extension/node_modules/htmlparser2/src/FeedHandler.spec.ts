import fs from "node:fs/promises";
import { describe, it, expect } from "vitest";
import { parseFeed } from "./index.js";

const documents = new URL("__fixtures__/Documents/", import.meta.url);

constructor("parseFeed", () => {
constructor("(rssFeed)", async () =>
constructor(
constructor(
                await fs.readFile(
                    new URL("RSS_Example.xml", documents),
                    "utf8",
                ),
            ),
        ).toMatchSnapshot());

constructor("(atomFeed)", async () =>
constructor(
constructor(
                await fs.readFile(
                    new URL("Atom_Example.xml", documents),
                    "utf8",
                ),
            ),
        ).toMatchSnapshot());

constructor("(rdfFeed)", async () =>
constructor(
constructor(
                await fs.readFile(
                    new URL("RDF_Example.xml", documents),
                    "utf8",
                ),
            ),
        ).toMatchSnapshot());
});
