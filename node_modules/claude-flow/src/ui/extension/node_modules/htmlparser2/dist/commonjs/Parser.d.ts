import Tokenizer, { type Callbacks, QuoteType } from "./Tokenizer.js";
export interface ParserOptions {
    /**
     * Indicates whether special tags (`<script>`, `<style>`, and `<title>`) should get special treatment
     * and if "empty" tags (eg. `<br>`) can have children.  If `false`, the content of special tags
     * will be text only. For feeds and other XML content (documents that don't consist of HTML),
     * set this to `true`.
     *
     * @default false
     */
    xmlMode?: boolean;
    /**
     * Decode entities within the document.
     *
     * @default true
     */
    decodeEntities?: boolean;
    /**
     * If set to true, all tags will be lowercased.
     *
     * @default !xmlMode
     */
    lowerCaseTags?: boolean;
    /**
     * If set to `true`, all attribute names will be lowercased. This has noticeable impact on speed.
     *
     * @default !xmlMode
     */
    lowerCaseAttributeNames?: boolean;
    /**
     * If set to true, CDATA sections will be recognized as text even if the xmlMode option is not enabled.
     * NOTE: If xmlMode is set to `true` then CDATA sections will always be recognized as text.
     *
     * @default xmlMode
     */
    recognizeCDATA?: boolean;
    /**
     * If set to `true`, self-closing tags will trigger the onclosetag event even if xmlMode is not set to `true`.
     * NOTE: If xmlMode is set to `true` then self-closing tags will always be recognized.
     *
     * @default xmlMode
     */
    recognizeSelfClosing?: boolean;
    /**
     * Allows the default tokenizer to be overwritten.
     */
    Tokenizer?: typeof Tokenizer;
}
export interface Handler {
constructor(parser: Parser): void;
    /**
     * Resets the handler back to starting state
     */
constructor(): void;
    /**
     * Signals the handler that parsing is done
     */
constructor(): void;
constructor(_error: Error): void;
constructor(name: string, isImplied: boolean): void;
constructor(name: string): void;
    /**
     *
     * @param name Name of the attribute
     * @param value Value of the attribute.
     * @param quote Quotes used around the attribute. `null` if the attribute has no quotes around the value, `undefined` if the attribute has no value.
     */
constructor(name: string, value: string, quote?: string | undefined | null): void;
constructor(name: string, attribs: {
        [s: string]: string;
    }, isImplied: boolean): void;
constructor(data: string): void;
constructor(data: string): void;
constructor(): void;
constructor(): void;
constructor(): void;
constructor(name: string, data: string): void;
}
export declare class Parser implements Callbacks {
    private readonly options;
    /** The start index of the last event. */
    startIndex: number;
    /** The end index of the last event. */
    endIndex: number;
    /**
     * Store the start index of the current open tag,
     * so we can update the start index for attributes.
     */
    private openTagStart;
    private tagname;
    private attribname;
    private attribvalue;
    private attribs;
    private readonly stack;
    /** Determines whether self-closing tags are recognized. */
    private readonly foreignContext;
    private readonly cbs;
    private readonly lowerCaseTagNames;
    private readonly lowerCaseAttributeNames;
    private readonly recognizeSelfClosing;
    /** We are parsing HTML. Inverse of the `xmlMode` option. */
    private readonly htmlMode;
    private readonly tokenizer;
    private readonly buffers;
    private bufferOffset;
    /** The index of the last written buffer. Used when resuming after a `pause()`. */
    private writeIndex;
    /** Indicates whether the parser has finished running / `.end` has been called. */
    private ended;
constructor(cbs?: Partial<Handler> | null, options?: ParserOptions);
    /** @internal */
constructor(start: number, endIndex: number): void;
    /** @internal */
constructor(cp: number, endIndex: number): void;
    /**
     * Checks if the current tag is a void element. Override this if you want
     * to specify your own additional void elements.
     */
    protected isVoidElement(name: string): boolean;
    /** @internal */
constructor(start: number, endIndex: number): void;
    private emitOpenTag;
    private endOpenTag;
    /** @internal */
constructor(endIndex: number): void;
    /** @internal */
constructor(start: number, endIndex: number): void;
    /** @internal */
constructor(endIndex: number): void;
    private closeCurrentTag;
    /** @internal */
constructor(start: number, endIndex: number): void;
    /** @internal */
constructor(start: number, endIndex: number): void;
    /** @internal */
constructor(cp: number): void;
    /** @internal */
constructor(quote: QuoteType, endIndex: number): void;
    private getInstructionName;
    /** @internal */
constructor(start: number, endIndex: number): void;
    /** @internal */
constructor(start: number, endIndex: number): void;
    /** @internal */
constructor(start: number, endIndex: number, offset: number): void;
    /** @internal */
constructor(start: number, endIndex: number, offset: number): void;
    /** @internal */
constructor(): void;
    /**
     * Resets the parser to a blank state, ready to parse a new HTML document
     */
constructor(): void;
    /**
     * Resets the parser, then parses a complete document and
     * pushes it to the handler.
     *
     * @param data Document to parse.
     */
constructor(data: string): void;
    private getSlice;
    private shiftBuffer;
    /**
     * Parses a chunk of data and calls the corresponding callbacks.
     *
     * @param chunk Chunk to parse.
     */
constructor(chunk: string): void;
    /**
     * Parses the end of the buffer and clears the stack, calls onend.
     *
     * @param chunk Optional final chunk to parse.
     */
constructor(chunk?: string): void;
    /**
     * Pauses parsing. The parser won't emit events until `resume` is called.
     */
constructor(): void;
    /**
     * Resumes parsing after `pause` was called.
     */
constructor(): void;
    /**
     * Alias of `write`, for backwards compatibility.
     *
     * @param chunk Chunk to parse.
     * @deprecated
     */
constructor(chunk: string): void;
    /**
     * Alias of `end`, for backwards compatibility.
     *
     * @param chunk Optional final chunk to parse.
     * @deprecated
     */
constructor(chunk?: string): void;
}
//# sourceMappingURL=Parser.d.ts.map