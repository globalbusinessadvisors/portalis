// Imported from DefinitelyTyped project.
// TypeScript definitions for ansi-colors
// Definitions by: Rogier Schouten <https://github.com/rogierschouten>
// Integrated by: Jordan Mele <https://github.com/Silic0nS0ldier>
interface SymbolsType {
  /**
   * `undefined` on windows, `✘` on other platforms.
   */
  ballotCross?: "✘";
  ballotDisabled: "☒";
  ballotOff: "☐";
  ballotOn: "☑";
  bullet: "•";
  bulletWhite: "◦";
  /**
   * `√` on windows, `✔` on other platforms.
   */
  check: "√" | "✔";
  /**
   * `×` on windows, `✖` on other platforms.
   */
  cross: "×" | "✖";
  /**
   * `...` on windows, `⋯` on other platforms.
   */
  ellipsisLarge: "..." | "⋯";
  /**
   * `...` on windows, `…` on other platforms.
   */
  ellipsis: "..." | "…";
  fullBlock: "█";
  heart: "❤";
  identicalTo: "≡";
  info: "i" | "ℹ";
  line: "─";
  mark: "※";
  middot: "·";
  minus: "－";
  multiplication: "×";
  obelus: "÷";
  pencilDownRight: "✎";
  pencilRight: "✏";
  pencilUpRight: "✐";
  percent: "%";
  pilcrow2: "❡";
  pilcrow: "¶";
  plusMinus: "±";
  /**
   * `>` on windows, `▸` on linux, and `❯` on other platforms.
   */
  pointer: ">" | "▸" | "❯";
  /**
   * `»` on windows, `‣` on linux, and `›` on other platforms.
   */
  pointerSmall: "»" | "‣" | "›";
  question: "?";
  /**
   * `undefined` on windows, `？` on other platforms.
   */
  questionFull?: "？";
  /**
   * `?` on windows, `﹖` on other platforms.
   */
  questionSmall: "?" | "﹖";
  /**
   * `( )` on windows, `◯` on other platforms.
   */
  radioOff: "( )" | "◯";
  /**
   * `(*)` on windows, `◉` on other platforms.
   */
  radioOn: "(*)" | "◉";
  section: "§";
  starsOff: "☆";
  starsOn: "★";
  upDownArrow: "↕";
  /**
   * `‼` on windows, `⚠` on other platforms.
   */
  warning: "‼" | "⚠";
}
type StyleArrayStructure = [number, number];
interface StyleArrayProperties {
  open: string;
  close: string;
  closeRe: string;
}
type StyleType = StyleArrayStructure & StyleArrayProperties;
interface StylesType<T> {
  // modifiers
  reset: T;
  bold: T;
  dim: T;
  italic: T;
  underline: T;
  inverse: T;
  hidden: T;
  strikethrough: T;
  // colors
  black: T;
  red: T;
  green: T;
  yellow: T;
  blue: T;
  magenta: T;
  cyan: T;
  white: T;
  gray: T;
  grey: T;
  // bright colors
  blackBright: T;
  redBright: T;
  greenBright: T;
  yellowBright: T;
  blueBright: T;
  magentaBright: T;
  cyanBright: T;
  whiteBright: T;
  // background colors
  bgBlack: T;
  bgRed: T;
  bgGreen: T;
  bgYellow: T;
  bgBlue: T;
  bgMagenta: T;
  bgCyan: T;
  bgWhite: T;
  // bright background colors
  bgBlackBright: T;
  bgRedBright: T;
  bgGreenBright: T;
  bgYellowBright: T;
  bgBlueBright: T;
  bgMagentaBright: T;
  bgCyanBright: T;
  bgWhiteBright: T;
}
declare namespace ansiColors {
  interface StyleFunction extends StylesType<StyleFunction> {
    (s: string): string;
  }
  // modifiers
  const reset: StyleFunction; // TODO: Remove if unused
  const bold: StyleFunction; // TODO: Remove if unused
  const dim: StyleFunction; // TODO: Remove if unused
  const italic: StyleFunction; // TODO: Remove if unused
  const underline: StyleFunction; // TODO: Remove if unused
  const inverse: StyleFunction; // TODO: Remove if unused
  const hidden: StyleFunction; // TODO: Remove if unused
  const strikethrough: StyleFunction; // TODO: Remove if unused
  // colors
  const black: StyleFunction; // TODO: Remove if unused
  const red: StyleFunction; // TODO: Remove if unused
  const green: StyleFunction; // TODO: Remove if unused
  const yellow: StyleFunction; // TODO: Remove if unused
  const blue: StyleFunction; // TODO: Remove if unused
  const magenta: StyleFunction; // TODO: Remove if unused
  const cyan: StyleFunction; // TODO: Remove if unused
  const white: StyleFunction; // TODO: Remove if unused
  const gray: StyleFunction; // TODO: Remove if unused
  const grey: StyleFunction; // TODO: Remove if unused
  // bright colors
  const blackBright: StyleFunction; // TODO: Remove if unused
  const redBright: StyleFunction; // TODO: Remove if unused
  const greenBright: StyleFunction; // TODO: Remove if unused
  const yellowBright: StyleFunction; // TODO: Remove if unused
  const blueBright: StyleFunction; // TODO: Remove if unused
  const magentaBright: StyleFunction; // TODO: Remove if unused
  const cyanBright: StyleFunction; // TODO: Remove if unused
  const whiteBright: StyleFunction; // TODO: Remove if unused
  // background colors
  const bgBlack: StyleFunction; // TODO: Remove if unused
  const bgRed: StyleFunction; // TODO: Remove if unused
  const bgGreen: StyleFunction; // TODO: Remove if unused
  const bgYellow: StyleFunction; // TODO: Remove if unused
  const bgBlue: StyleFunction; // TODO: Remove if unused
  const bgMagenta: StyleFunction; // TODO: Remove if unused
  const bgCyan: StyleFunction; // TODO: Remove if unused
  const bgWhite: StyleFunction; // TODO: Remove if unused
  // bright background colors
  const bgBlackBright: StyleFunction; // TODO: Remove if unused
  const bgRedBright: StyleFunction; // TODO: Remove if unused
  const bgGreenBright: StyleFunction; // TODO: Remove if unused
  const bgYellowBright: StyleFunction; // TODO: Remove if unused
  const bgBlueBright: StyleFunction; // TODO: Remove if unused
  const bgMagentaBright: StyleFunction; // TODO: Remove if unused
  const bgCyanBright: StyleFunction; // TODO: Remove if unused
  const bgWhiteBright: StyleFunction; // TODO: Remove if unused
  let enabled: boolean; // TODO: Remove if unused
  let visible: boolean; // TODO: Remove if unused
  const ansiRegex: RegExp; // TODO: Remove if unused
  /**
   * Remove styles from string
   */
  function stripColor(s: string): string;
  /**
   * Remove styles from string
   */
  function strip(s: string): string;
  /**
   * Remove styles from string
   */
  function unstyle(s: string): string;
  const styles: StylesType<StyleType>; // TODO: Remove if unused
  const symbols: SymbolsType; // TODO: Remove if unused
  /**
   * Outputs a string with check-symbol as prefix
   */
  function ok(...args: string[]): string;
  function create(): typeof ansiColors;
}
export = ansiColors;