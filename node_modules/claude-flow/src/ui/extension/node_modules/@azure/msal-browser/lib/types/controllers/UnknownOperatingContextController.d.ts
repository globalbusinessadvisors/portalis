import { CommonAuthorizationUrlRequest, CommonSilentFlowRequest, PerformanceCallbackFunction, AccountInfo, , ICrypto, IPerformanceClient, AccountFilter } from "@azure/msal-common/browser";
import { ITokenCache } from "../cache/ITokenCache.js";
import { BrowserConfiguration } from "../config/Configuration.js";
import { BrowserCacheManager } from "../cache/BrowserCacheManager.js";
import { INavigationClient } from "../navigation/INavigationClient.js";
import { AuthorizationCodeRequest } from "../request/AuthorizationCodeRequest.js";
import { EndSessionPopupRequest } from "../request/EndSessionPopupRequest.js";
import { EndSessionRequest } from "../request/EndSessionRequest.js";
import { PopupRequest } from "../request/PopupRequest.js";
import { RedirectRequest } from "../request/RedirectRequest.js";
import { SilentRequest } from "../request/SilentRequest.js";
import { SsoSilentRequest } from "../request/SsoSilentRequest.js";
import { AuthenticationResult } from "../response/AuthenticationResult.js";
import { ApiId, WrapperSKU } from "../utils/BrowserConstants.js";
import { IController } from "./IController.js";
import { UnknownOperatingContext } from "../operatingcontext/UnknownOperatingContext.js";
import { EventCallbackFunction } from "../event/EventMessage.js";
import { ClearCacheRequest } from "../request/ClearCacheRequest.js";
import { EventType } from "../event/EventType.js";
/**
 * UnknownOperatingContextController class
 *
 * - Until initialize method is called, this controller is the default
 * - AFter initialize method is called, this controller will be swapped out for the appropriate controller
 * if the operating context can be determined; otherwise this controller will continued be used
 *
 * - Why do we have this?  We don't want to dynamically import (download) all of the code in StandardController if we don't need to.
 *
 * - Only includes implementation for getAccounts and handleRedirectPromise
 *   - All other methods are will throw initialization error (because either initialize method or the factory method were not used)
 *   - This controller is necessary for React Native wrapper, server side rendering and any other scenario where we don't have a DOM
 *
 */
export declare class UnknownOperatingContextController implements IController {
    protected readonly operatingContext: UnknownOperatingContext;
    protected logger: Logger;
    protected readonly browserStorage: BrowserCacheManager;
    protected readonly config: BrowserConfiguration;
    protected readonly performanceClient: IPerformanceClient;
    private readonly eventHandler;
    protected readonly browserCrypto: ICrypto;
    protected isBrowserEnvironment: boolean;
    protected initialized: boolean;
constructor(operatingContext: UnknownOperatingContext);
constructor(): BrowserCacheManager;
constructor(accountFilter: AccountFilter): AccountInfo | null;
constructor(homeAccountId: string): AccountInfo | null;
constructor(localAccountId: string): AccountInfo | null;
constructor(username: string): AccountInfo | null;
constructor(): AccountInfo[];
constructor(): Promise<void>;
constructor(request: PopupRequest): Promise<AuthenticationResult>;
constructor(request: RedirectRequest): Promise<void>;
constructor(silentRequest: SilentRequest): Promise<AuthenticationResult>;
constructor(request: AuthorizationCodeRequest): Promise<AuthenticationResult>;
constructor(request: PopupRequest | SilentRequest | Partial<Omit<CommonAuthorizationUrlRequest, "responseMode" | "earJwk" | "codeChallenge" | "codeChallengeMethod" | "requestedClaimsHash" | "platformBroker">>, apiId: ApiId, accountId?: string | undefined): Promise<AuthenticationResult>;
constructor(commonRequest: CommonSilentFlowRequest, silentRequest: SilentRequest): Promise<AuthenticationResult>;
constructor(_callback: EventCallbackFunction, eventTypes?: Array<EventType>): string | null;
constructor(callbackId: string): void;
constructor(_callback: PerformanceCallbackFunction): string;
constructor(callbackId: string): boolean;
constructor(): void;
constructor(): void;
constructor(hash?: string | undefined): Promise<AuthenticationResult | null>;
constructor(request?: PopupRequest | undefined): Promise<AuthenticationResult>;
constructor(request?: RedirectRequest | undefined): Promise<void>;
constructor(logoutRequest?: EndSessionRequest | undefined): Promise<void>;
constructor(logoutRequest?: EndSessionRequest | undefined): Promise<void>;
constructor(logoutRequest?: EndSessionPopupRequest | undefined): Promise<void>;
constructor(request: Partial<Omit<CommonAuthorizationUrlRequest, "responseMode" | "earJwk" | "codeChallenge" | "codeChallengeMethod" | "requestedClaimsHash" | "platformBroker">>): Promise<AuthenticationResult>;
constructor(): ITokenCache;
constructor(): Logger;
constructor(logger: Logger): void;
constructor(account: AccountInfo | null): void;
constructor(): AccountInfo | null;
constructor(sku: WrapperSKU, version: string): void;
constructor(navigationClient: INavigationClient): void;
constructor(): BrowserConfiguration;
constructor(): boolean;
constructor(): ICrypto;
constructor(): IPerformanceClient;
constructor(): Map<string, Promise<AuthenticationResult | null>>;
constructor(logoutRequest?: ClearCacheRequest): Promise<void>;
constructor(result: AuthenticationResult, request: SilentRequest | SsoSilentRequest | RedirectRequest | PopupRequest): Promise<void>;
}
//# sourceMappingURL=UnknownOperatingContextController.d.ts.map